# P2 目前采样最好的一份读数：43 条可判定，par 仍为 0（2026-07-30）

联合闸（可达优先 / 质量其次）+ 受让人补齐后的 59 条跑满。
**红线① 前后 CODE V 会话数均为 0。**

## 读数

```
trials 59   par 0   worse 43   spec_not_met 5   unmeasurable 11
par rate / all trials 0.0%      par rate / judged only 0.0%
distinct CONTROL designs 37     distinct SEED designs 6
```

**⇒ 异源打平率 = 0%，可判定 43 条 / 独立对照设计 37 个。**
这是目前采样最好的一份，也是判据②要的那个数。

## 与基线对比（同一台机器、同一份 census）

| | 基线（旧池 49 条） | 联合闸（59 条） |
|---|---|---|
| 可判定 | 35 | **43** |
| **par** | **0** | **0** |
| 独立对照设计 | 27 | **37** |
| 两侧全视场 | 40/49 | 48/59 |
| RMS 单项 par | 3 | **4** |
| MTF 单项 par | 1 | 1 |
| 畸变单项 par | 0 | **0** |
| 中位单条墙钟 | 178.2s | 212.2s |

**可判定从 35 涨到 43，主要来自受让人补齐带来的 10 条新 trial，不是闸的功劳。**

## 预测已先行写下并被证实

改闸之前我先写了预测：**联合闸只改了 5/59 条配对，所以 par 结果应当与基线基本相同**
（`p1-root-cause-routing-gate-2026-07-30.md` 补节）。实测 par 仍为 0 ⇒ 预测成立。
先写预测的意义在于：**这个 0 不能被读成「闸没用」** ——
闸是正确性修复（把 3/59 条需要不可能拉焦的配对清零），不是打平率的杠杆。

## 仍然没有一条同时打平三项

RMS 单项 par **4** 条、MTF **1** 条、畸变 **0** 条。畸变是最硬的一项，
和基线一致。**差距不是均匀的。**

## 这份读数指向的下一步

结合 `reachability-quality-conflict-2026-07-30.md` 与 PR #161 的权重敏感度实测
（**池子大小 `{4:45, 47:2, 48:2}`，92% 的对照在 4 颗里挑 1 颗，整张权重网格纹丝不动**）：

**绑定约束是可选 seed 太少，不是打分、不是闸、不是权重。**
59 个对照里只有 6 个存在「既在 +25% 拉焦内、又达语料中位像质」的异源 seed。

⇒ **扩底库是目前唯一有杠杆的方向。** 另一条是把 +25% 这个能力上限本身往外推。

## 诚实边界

- `--skip-tolerance`，所以四件套 `all_four` 报 `not_assessable`（55/59 按请求跳过），
  不是 0。公差那一件仍未在两侧同时测出过（见 `project-tolerance-chain-three-defects`）。
- 选优偏差在这一批仍是 **RMS 决定 25 条、中位增益 +71.5%**，与基线同量级。
