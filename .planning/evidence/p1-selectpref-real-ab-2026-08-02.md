# P1 真机轮：列车修复的 A/B 答案——无害，但在本计划上不是杠杆（2026-08-02）

主公 08-02 裁定「真机时间给 P1，发车」。本轮回答审计留下的问题：随 #155 列车落 main 的
三项修复（`_select_preferred` 丢场惩罚 + `@rmsnf` 见证宏 + 联合 seed 闸）在真机上对
产出能力的实际影响。

## 运行设置（与 2026-07-30 基线可比）

- 同机、同 census（`trace-census-20260728/perfield-census.jsonl`）、同 49 条计划
  （5 颗 seed，US-12044826-B2-e4 承担 41/49）；代码 = main `08017484`（含列车 + #166 + #167）。
- `--skip-tolerance`（真机时间全给 P1；四件套完整度按名记 not assessable）。
- **红线① pre-run 与 post-run CODE V 会话数均为 0**（脚本自证，日志留痕）；
  运行全程第二实例监控零告警。
- 输出：`D:\atelier-p1-runs\p1-selectpref-ab-20260802\`；中位单条墙钟 236.0s（基线 178.2s）。

## 读数对比（基线 = 2026-07-30 旧池 49 条）

| | 基线 07-30 | 本轮 08-02 | 动了吗 |
|---|---|---|---|
| par | 0 | **0** | 没动 |
| judged (worse) | 35 | **35** | 没动 |
| 两侧全视场 | 40/49 | **40/49**（cand_partial 3 / cand_zero 6） | 没动 |
| RMS 单项 par | 3 | **3** | 没动 |
| MTF 单项 par | 1 | **1** | 没动 |
| 候选 RMS 落败中位 | 16.7×（14 条可判定臂） | **16.1×**（n=35，区间 0.04–54） | 统计上没动 |
| 候选 MTF 比中位 | 0.022× | **0.020×**（n=31） | 没动 |
| 独立对照设计 | 27 | 27 | 没动 |

⚠️ 中途 n=6 时 RMS 中位曾读 12.3×——跑满后回到 16.1×，**小样本假象第四次现身**，
又一次证明中途读数不可当结论（[[feedback-measurement-traps]] 第 2 条）。

## 本轮的真答案：选择器修复从未成为决策因子

新见证仪表（本轮首次在真机出数）：

```
config choice decided by:  aut_not_converged=17,  rms_spot=19,  tie_fixed_priority=1
  RMS gain when RMS chose:  median +71.5% (n=19)
field witness shortfall:    both_full=40, candidate_partial=3, candidate_zero=6
```

**`_select_preferred` 的丢场惩罚项在 37 次决策中 0 次成为决定因素**——决策全部由
「一侧未收敛」（17）与「RMS 更优」（19）先行拍板。机制在、场合没出现：裁瞳修复
（#146，已在基线里）把「优化器造出丢场配置」的发生率压下去之后，选择器的第 4 排序项
几乎无用武之地。修复**无害**（RMS 决策时中位 +71.5% 的增益证明选择器本身在正确工作），
但**不是 P1 的杠杆**。

## 连带证认

- **对照身份病灶再次实锤**：`US-12436366-B2-e10`（存量 2.38µm / CODE V 447.6µm 的
  188× 案例，PR #159）作为对照出场——候选 16.5µm 全场 2/2 在 RMS 上赢它（单项 par），
  仍因 MTF/畸变判 worse。读数全部健康（`withheld: []`），不是退化值第 12 例。
  存量像质重锚（PR #159 两条出路）仍未做，这颗烂对照就仍在池里。
- blocked_at 分布：aut_not_converged 5 / control_engine_disagreement 2 /
  optimize_no_preferred 2（unmeasurable 9 条的根因形态，与基线一致）。

## 结论与下一杠杆

三项修复的真机 A/B 判定：**无回归、无增益——P1 的一阶瓶颈不在选择/测量链路**。
这与审计的深层结论闭环：瓶颈在**上游供给**——
① 底库供不出既在 +25% 拉焦内又达语料中位像质的 seed（6/59）；
② 对照身份与存量像质未重锚（447µm 假 2.38µm 仍在服役）；
③ SCENARIO_BOUNDS（放宽 = 独立样本 37→128，待主公裁定）。
真机时间继续砸在链路端已无增量，下一铲应转向语料重生成/存量像质重锚（自主格内）
与 bounds 裁定（主公格）。
