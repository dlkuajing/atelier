# 北极星四判据 · 2026-08-04 实测汇总

**取代** `.planning/evidence/north-star-scoreboard-2026-07-30.md`（那一版的 P2 段已被
真机推翻：它写「候选落败幅度 RMS 中位 16.7×」，现在候选在 RMS 上**赢** 45/48）。

**本页所有数字出自同一批真机**：`D:/atelier-p1-runs/p3-fourpiece-20260804`
（全量 59 条带公差，代码 `89c5011a`，红线① pre/post 均 0 且**全程连续监控无 ALERT**），
加两个派生批次 `p2-distfixed{3.0,1.5,1.0}-20260804`。
三个待实测的数（`N` / 打平率门槛 / `T`）**按北极星规定仍不拍板**——下面的 N 与 T 是
**实测值，不是门槛**。

---

## P1 · 产出能力：第一次有 N / M / T

| | 值 | 分母口径 |
|---|---|---|
| **N** 一次运行覆盖的独立需求 | **59** | **37 个不同对照设计**（处方指纹）／**31 个不同需求 spec** |
| **M** 完整四件套交付物 | **49 on-spec**（产出四件的行数 50） | **50 行 = 30 个不同候选处方**（20 行重算了已存在的文件） |
| **T** 总耗时 | **1888 s = 31.5 min** | 按行中位 **24.9 s**；**按不同候选设计 62.9 s** |

**零介入**：无人选 seed、无人调参、无人工重试，`budget_exhausted 0`。
未进 M 的 9 条按名记录：`pupil_growth_not_reachable 7` / `control_engine_disagreement 2`。
第 10 条 `blocked_at`（`efl_not_matched`）四件齐全但**没打中被要求的焦距**（EFL +3.31%），
按判据①不算那条需求的交付物——产物同时印 `all_four 50 / on_spec_four 49`。

⚠️ **50 分钟的尾巴是真的，但它属于优化器不属于公差**：`four-piece-v2` 那条 51.2 min 的
trial，`optimize` 占 **3063.8 s / 3072.8 s = 99.7%**（autovig 梯级，rung 间隔 300 s）。

**本轮该尾巴已大幅收窄，但仍是 T 的主要不均匀来源**：

| `autovig_edge_used` | n | 耗时 | 占全批 | 中位/条 |
|---|---|---|---|---|
| **> 0（裁瞳梯级真的跑了）** | **3** | 413.3 s | **21.9%** | 112–189 s |
| = 0 | 47 | 1452.0 s | 76.9% | 26.5 s |
| None（没进优化） | 9 | 22.5 s | 1.2% | 2.3 s |

⇒ **5% 的 trial 吃掉 22% 的墙钟。** 这是 P1 的成本问题，至今未修。

## P2 · 异源打平率（主指标）：**实为 0**，且优化器这条路已封闭

```
59 trials | par 1 | worse 47 | spec_not_met 8 | unmeasurable 3
judged 48 行 = 29 个对照设计 / 9 个 seed 设计
  rms_spot_um     par 45/48      <- 候选赢
  mtf_min         par 44/48      <- 候选赢
  distortion_pct  par  1/48      <- 唯一失守项
```

**那条 par 不算数**：它的对照 RMS **97.97 µm**（语料中位 12 倍，全场第 57/59 差），
它的 seed 自己 `codev_rms 98.4394 µm`（seed 侧闸是 10.2312，它是 3 条兜底件之一）。
**98.4 µm 的 seed 打赢 97.97 µm 的对照，两边都不合格。**
加任何一档低于 97.97 的对照闸，par 归 0（50 µm → judged 44/par 0；10.2312 → 36/par 0）。

⇒ **诚实读数：打平率 0。** 门槛按北极星规定仍留空。

### 优化器这条路封闭，两条机制各有剂量-反应曲线

| 机制 | 最小档代价（候选 RMS） | 曲线 |
|---|---|---|
| 加权 WTC | w=0.01 → **×44.5** | 0.01 / 0.1 / 1.0 单调 |
| 固定硬约束 | B=3.0 → **×71.3**，MTF ×0.032 | 3.0 / 1.5 / 1.0 单调 |

约束**确实被引擎执行**（候选畸变中位精确落在 bound：2.9687 / 1.5002 / 0.9992），
但把畸变从 ~17% 压到 1.5% 要拿两个数量级的像质换。
**反例证明机制**：seed 基线畸变本就 0.804% 的那条，B=3.0/1.5 **零代价**。
⇒ **不是优化器弱，是 seed 的设计形式没有那个自由度。**

**根因是供给结构**：judged 对照 **47 LARGAN + 1 AAC** ⇒ 异源规则排除全部 LARGAN seed
⇒ 广角对照的合格池被 brand × EFL±25% × |Δfov| × rms 四闸钉死在
**SAMSUNG US-12560782-B2 一族**（一颗设计成桶形、靠软件后校正的超广角）。
**能改变 P2 的四件全是主公口径决策**，见 `PENDING-RULINGS.md` 第 00 项。

## P3 · 交付物四件套：第一次有数（此前恒 `not_assessable`）

| 口径 | 值 |
|---|---|
| 产出四件的行数 | **50/59** |
| …且打中自己的 spec（判据①算这个） | **49/59** |
| …且公差良率**有意义**（`yield_is_informative`） | **45/59** |

逐件：处方 ZMX 50 / 像质 50 / 公差良率 50 / 相对成本 50。
两侧**免阈值**敏感度都测到 **50/59**；对照侧绝对良率被 fail-closed 拒绝 **11/59**
（样本出 TOR 线性模型 >25%，是正确行为不是缺陷）。

**为什么现在能测**：TOR 要求参考光线可追迹；2026-07-30 那批候选 54 次全反射，
现在候选 RMS 中位 1.84 µm、**50/59 两侧都有波前读数**。

### 附带发现：候选比专利对照抗公差

同一张（未标定的）公差表两侧同施，RMS 波前退化 99.9% 档：

| 去重键 | n | 倍数 | 候选更稳 |
|---|---|---|---|
| 行 | 50 | 11.80× | 45/50 |
| **对照处方** | **31** | **8.47×** | **27/31** |
| seed | 10 | 3.07× | 7/10 |

⚠️ **三条限定缺一不可**：① **50 行里 43 行继承自同一件 seed 专利**，这目前是
「这一族 seed ＋ 我们的优化」的性质，**还不能记在 agent 头上**；
② 这 50 对里 **47 对判决是 `worse`**（候选畸变 17.40% vs 对照 1.93%），
且候选畸变越大公差优势越大（ρ=+0.53）⇒ **不是同一份要求下的稳健度**；
③ 对照 MC 被钳到精确 0.0 的有 39/50，只取两侧都在天花板内的 39 对 ⇒ **9.39×**。

## P4 · 可独立复核

见 `.planning/evidence/p4-second-engine-recheck-*`（本轮正在同批重算，结果另页）。
上一轮（2026-08-02，49 条那批）：`engine_failed` **0/188**，三项判据第一次有分布，
EFL n=94 中位 0.9997；⚠️ `F/# min=max=1.0` **是两个引擎把 ZMX 声明的 `FNUM` 读回来**，
不是独立算出同一个数（判别器：有散布⇒被算了，一位不差⇒被抄了）。

**本轮同一现象复现**：三项齐全的 50 行上 `候选 F/# == 对照 F/# == spec`，**50/50 零例外**
（跨行有 19 个不同取值），而同批 `efl_ratio` 有 **30 个互异值**。
⇒ **F/# 不构成一次独立通过的检查**，不得列入合规证据。

## T1 · 客户侧采用

**仍不可测**（无客户接触渠道）。P1–P4 全绿 = 够格拿去给客户看；T1 闭合 = 客户认了。
**在 T1 闭合前，任何材料不得表述为「已达成北极星」。**

---

## 复算

```
uv run python scripts/p2_crosssource_trial.py --run \
  --census D:/atelier-stagec-runs/trace-census-20260728/perfield-census.jsonl \
  --out <目录> --trial-budget-seconds 1800
uv run python scripts/p2_crosssource_trial.py --report --out D:/atelier-p1-runs/p3-fourpiece-20260804
uv run python scripts/codev_session_watch.py --interval 5 --max 1   # 红线①要求的后一半
```

明细：`p1-p3-first-full-reading-2026-08-04.md`、`distortion-bound-dose-response-2026-08-04.md`
