# Family 45593888 exact-source audit

## Binding and lineage

The retained official `US-8218254-B2` text binds Family ID `45593888`,
application `12/861419`, Jane L. Bareau, James D. Milks, Nidia A. Alvez,
Teresa M. Lupien and assignee Gyrus ACMI, Inc. Its only printed application
lineage is prior publication `US-20120044587-A1` for the same application. The
source prints no Related Application Data or priority claim. The
`PCT/US2011/048390` token occurs only in an examiner-cited international-search
report and is not promoted to lineage.

## Exact denominator

- Background/Summary has lines 1-12.
- Description has lines 1-44: brief-drawing lines 1-8 and detailed-description
  lines 9-44.
- Claims 1-12 are complete; claims 1, 4 and 12 are independent.
- FIGS. 1-6 are declared on six drawing sheets; the normalized source contains
  seventeen `FIG.` references.
- The HTML has no table tag or MathML object. Paragraph 42 contains the single
  flattened `TABLE-US-00001` prescription.
- Four exact source items reconcile without an unmapped item: the generic
  imaging-optic architecture, the reflected/transmitted-beam alignment and
  assembly method, the endoscope wrapper, and the numerical Example 1.
- FIG. 1 and Background/Summary lines 4-8 describe contextual prior art and are
  not counted as an inventive source item.

## Prescription availability

Example 1 directly prints one finite-object spherical/plane prescription with
eighteen ordered rows: `OBJ`, surfaces 1-6, `STO`, surfaces 8-16 and `IMA`.
The five columns are Surface, Radius, Thickness, Glass and Diameter. The source
also states that thickness and diameter are millimeters. It does not directly
publish Example 1 effective focal length, F-number, numeric angular field or
numeric image height.

The `IMA` row's printed `0.714` is in the Diameter column. It is retained as a
diameter; it is not renamed image height or divided by two. No paraxial solve,
finite-object substitution, plot/drawing measurement, numeric repair or value
from the prior A1 is used. Example 1 is therefore
`metadata_unpublished.prescription_specific_efl_f_number_angular_field_and_image_height_absent`.
The architecture, method and endoscope items publish no independent ordered
prescription and close as three distinct `confirmed_no_prescription` terminals.

## Original-raster audit

Two separately downloaded 775,186-byte official PDF containers have distinct
container hashes but decode to the same twelve 2560x3300 1-bit page rasters at
ordered raster-set hash `2de83d8f...95330`. Both have zero text-layer
characters. Pages 2-7 are the six drawing sheets; page 11 carries Example 1 and
its table; page 12 continues the specification and prints the claims. The
twelve retained PNGs are lossless 1-bit serializations whose decoded pixels
equal the embedded originals. The contact sheet is navigation-only.

No image enhancement, OCR repair, drawing measurement, table-value
transcription, plot sampling, raster inference, diameter halving or
related-publication borrowing occurred. No worker, conversion request/receipt,
fingerprint, candidate/staging ZMX, formal intake or CODE V run exists for this
family.
