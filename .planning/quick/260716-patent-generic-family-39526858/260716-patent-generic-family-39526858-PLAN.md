# Quick: Patent generic Family 39526858

**Status:** complete-shovel-saturation-incomplete
**Parent:** patent saturation engineering (active / incomplete)
**Started:** 2026-07-16
**Entry evidence:** strict 619/619 replay result set
`a20cd8538136baf3997754907163d225baba6b9d5cfc849fee1c6c9a63ec38c9`, with
`generic_summary_metadata_missing` at 165 items / 165 roots and no corrupt result.

## Objective

Resolve the deterministic next exact same-layout family cluster, Family ID `39526858`:
`US-20160088216` and `US-9699370`. Reconcile publication/application/family identities, every
declared example or embodiment, the complete table and drawing denominator, every required optical
system field, and official raster evidence before choosing deterministic conversion, a
source-proven terminal, or continued parser review. The shared layout signature is queue evidence
only.

## Plan

1. Freeze the 165-item generic census and source-lock both official documents, their relationship,
   source denominator, and current two-item failure set.
2. Audit every table, figure, example/embodiment anchor, surface/asphere row, optical layout, and
   system-metadata binding; acquire official raster evidence for image-only content if required.
3. Implement only exact-source fail-closed behavior. Never infer, repair, interpolate, or derive a
   missing optical value from a drawing, formula, range, or family peer.
4. Replay both roots append-only twice, compare canonical semantics excluding only permitted retry
   identity, audit all 619 roots, and rebuild the after generic census twice.
5. Run offline patent/guard tests, Ruff, and `git diff --check` with `PYTHONUTF8=1`, active
   non-`real_machine` guard, and CODE V inventory zero.
6. Update STATE, decisions, this plan, and the recovery cursor with exact hashes. Commit this
   shovel, then remeasure all executable buckets. Parent saturation remains incomplete.

## Completion evidence

- Exact official `US-20160088216-A1` and `US-9699370-B2` records share application `14/958173`,
  Family ID `39526858`, title, owner, continuation/divisional chain, and provisional roots; B2
  Prior Publication Data names A1. Raw and normalized HTML hashes are pinned independently.
- Both sources declare one formal Example 1, four lettered PPUBS tables, and FIGS.1-28. TABLE A is
  material data, B is force-profile behavior, C is the fitted actuator's focus response, and D is
  terminal lens-setting control. The generic census missed them because its table grammar requires
  numeric labels; the exact classifier binds all four without altering the global census ruler.
- Example 1 fits the actuator to an existing IT5000 triplet. The stated 5.88 mm focal length,
  F# 6.6, and 36-inch nominal focus describe that external assembly; no ordered radii, spacings,
  glasses/Abbe values, conics, aspheres, surface numbers, or numeric field are published.
- The official A1/B2 PDFs have 37/39 image-only pages and 16 drawing sheets each. Two live wrappers
  differ per publication, but every decoded raster is equal within the publication. All-page
  contact sheets and full-resolution table pages establish the complete table/page denominator.
- Each root produces one
  `confirmed_no_prescription.deformable_lens_actuator_and_imaging_terminal_architecture_only`
  terminal with no worker, request, receipt, fingerprint, candidate, or ZMX. Source, table,
  drawing, and injected prescription-marker drift all reopen parser review.
- Six parent-chain publication/grant roots named in cross-reference paragraph `[0001]` are outside
  the frozen cohort and are durably queued after the frozen 619 roots; later incorporated but
  non-chain references are not silently promoted.
- Attempts 2/3 are append-only and semantic-equal after excluding only `result_attempt`.
  `US-20160088216` has semantic SHA-256
  `f49fe18bb830166ea266dad48716e09036eb5c790017735495286ce25a571102`;
  `US-9699370` has `a7eae05d7901b15cc1ed74c7ca0c54341b2fc82eb5cd6416df670bfb77635a2a`.
- Result set is `d1e244e12272e9752d3771da5e2d9ac1b193eae8e85983635054b4f5b0029981`.
  Strict audit is 619/619, missing=0, corrupt=0. Summary/report SHA-256 values are
  `3c060a840ff1a7442513ad8209c7043fbbd9d6dff2b1a1467f9b0bf4bc6d2351` and
  `541a6ea105691302678374343f088b996f89803d9890de78495a8ed9db1518f5`.
- Generic census falls from 165 items/roots to 163/163. Two fresh after builds are byte-identical at
  `334d9fb747e3f9c2adb3f2003a6f41291b109dab0c8f06ead8b96c1a5dfb7ce5`.
- All 369 offline patent parser/replay/census/saturation/process/pool/crawl-QC/guard tests, Python
  compilation, Ruff, and `git diff --check` pass with CODE V inventory zero.
- Deterministic queue recomputation keeps generic first at 163 nonterminal roots. The next exact
  two-root family is Family ID `59199108`: `US-12298484` and `US-20240411113`, layout
  `8e771ac86c6c9e65092359268021f602cbdf709bedc6e8d98f5dc40574d0230e`.

Parent patent saturation, the external-family queue, and formal intake remain incomplete. Full
host pytest is not claimed; complete integration truth remains GitHub CI.
