# Family 63853856 source audit

`US-10514523-B2` is the retained patent publication for application `15/671765`
in this replay root. The exact official USPTO HTML is the numeric authority. Its
raw SHA-256 is
`0c93198ad6094f8057d5df45a399a8bbd615f6286790c5a87632a1447a81b902`;
the runtime-normalized text has 168,959 characters and SHA-256
`364862001802743e678893603c8985b69a128ce8c810324a64cfac7ddb8b8303`.
No related-family value, drawing measurement, or raster numeric transcription was
used.

## Closed denominator

The publication contains 47 numbered background/summary paragraphs, 243 numbered
description paragraphs (33 brief-drawing and 210 detailed), 27 claims (1, 12, 22,
and 24 are independent), 32 declared figure panels, 28 flattened `TABLE-US`
objects, 12 formal optical tables, two inline-formula processing instructions, no
MathML, and three custom-character image tags. Tables 1/3/5/7/9/11 are six ordered
surface prescriptions; Tables 2/4/6/8/10/12 are their asphere coefficients.

The item denominator is 14: six optical prescriptions plus eight distinct
electronic-device deployment wrappers in paragraph 242 and Figures 7A-7H. The six
prescriptions publish 103 ordered rows including object and image. Items 1/4/5/6
have directly published `FST/HEP`, HAF, stop, wavelength, surface, material, and
asphere data and produce 61 modeled sequential surfaces with 42 aspheres. Items 2
and 3 print `FST/REP`; `REP` occurs only in those two labels and is never defined,
so both terminate `metadata_unpublished` rather than being repaired to `HEP`.
The eight deployment wrappers publish no additional prescription. Every paragraph,
claim, figure panel, table, formula instruction, image tag, and source item is
mapped.

## Exact source exceptions

The parser retains the printed anomalies `FST/REP`, `Table 9 and Table in`,
`Aperature`, `Imagez plane`, `6.sup.th ens`, the Table 7 auxiliary unit placeholder,
and all three custom-character placeholders. Three narrow lexical normalizations
are source-locked: the first embodiment's filter row is bound to glass from its
same-item prose; the flattened sixth-embodiment `plastic` token is rebound to the
explicitly identified second lens; and terminal `Image plane`/`Imagez plane` labels
become a zero-thickness `Image` surface. No numeric token is changed.

## Official raster review boundary

Two official 3,785,664-byte PDF fetches have different container SHA-256 values
(`52d8bf06234a9042a59ab92b686b85ae63999e8c285285eb935a92d4b6bae6da`
and
`903ec3f725c4a6eddb2a191f4b2a2a4be2065aaf35e0f4d41b4125330f31836b`)
but decode to the same 63 one-raster, zero-text pages. Their canonical decoded
raster-set SHA-256 is
`57db7ce6fcff8f685ee7221895aaefb2294fff589f6a23381288aec4ae4a60f4`.
All 63 retained original page PNGs were visually reviewed: page 1 is the cover,
pages 2-33 are drawing sheets, pages 34-63 are specification pages, tables occur
on pages 46-60, and claims occupy pages 60-63. Contact sheets were navigation-only;
there was no enhancement, measurement, OCR repair, or raster numeric inference.

## Replay outcome

The family-keyed parser emits exactly 14 item attempts. Four process-isolated
conversions are `converted_pending_intake`, two items are terminal
`metadata_unpublished`, and eight wrappers are terminal
`confirmed_no_prescription`. Two append-only replays have equal canonical business
semantics
(`47bd073b39157f754631ad36112da43ec1ce9a3c32b4ba0f63c2a952779afbfb`)
and byte-identical request, response, candidate, stdout, stderr, and staging ZMX
payloads. No CODE V call or formal intake occurred.

The strict ledger remains complete at 619/619 roots with zero missing or corrupt
results. Repeated censuses are byte-identical and reduce the generic residual from
63 to 62 roots/items. Patent saturation is not complete; the next exact group is
Family 85177416 / `US-20230048740-A1` under the minimum-layout-signature ordering
rule.
