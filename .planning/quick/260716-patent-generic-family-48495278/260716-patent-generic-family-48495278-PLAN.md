# Quick Task: Patent Generic Family 48495278

**Status:** complete-shovel-saturation-incomplete
**Parent:** patent saturation engineering (active / incomplete)
**Started:** 2026-07-16
**Entry evidence:** strict 619/619 replay result set
`9a5bb4f9f35a0ac531d53cc02e0dd0b6d08ef7b6bbb55cf41f287270e7223da3`, with
`generic_summary_metadata_missing` at 157 items / 157 roots and no corrupt result.

## Objective

Resolve the deterministic next exact-family cluster, Family ID `48495278`:
`US-20150138653` and `US-8976467`. Reconcile the continuation/publication lineage, all six
declared four-lens embodiments, FIGS. 1-28, every raster-only optical/asphere table, comparison
table, mobile-device wrapper, and every official source page before choosing deterministic
conversion, a source-proven terminal, or continued parser review. The shared layout signature is
queue evidence only.

## Plan

1. Freeze the 157-item generic census and source-lock both official documents, their lineage,
   source denominator, and current two-item failure set.
2. Audit all six FIG. 4/5 through FIG. 24/25 optical/asphere table pairs, FIG. 26 comparison data,
   FIGS. 1-28, every official PDF page, and any OCR overlay only after decoded-page raster
   equivalence.
3. Reuse or extend an exact-source parser only when this exact family satisfies every identity,
   page, figure, label, and numeric-confidence gate. Never infer, repair, interpolate, derive, or
   borrow a missing cell or system value.
4. Replay both roots append-only twice, compare canonical semantics excluding only permitted retry
   identity, audit all 619 roots, and rebuild the after generic census twice.
5. Run offline patent/guard tests, Ruff, Python compilation, and `git diff --check` with
   `PYTHONUTF8=1`, active non-`real_machine` guard, and CODE V inventory zero.
6. Update STATE, decisions, this plan, and the recovery cursor with exact hashes. Commit this
   shovel, then remeasure all executable buckets. Parent saturation remains incomplete.

## Entry facts

- `US-20150138653-A1` is application `14/608769`, filed 2015-01-29, and declares itself a
  continuation of application `13/757675`; its retained official HTML SHA-256 is
  `cc17913116d0dc5ee3b49fcaf720e69824f8fd1cf2ea4c79aa94e8ae1c1da145`.
- `US-8976467-B2` is application `13/757675`, filed 2013-02-01, and names prior publication
  `US-20140071340-A1`; its retained official HTML SHA-256 is
  `dc2eefd750653fe96b856789b279f2fe8b461cdf13fad7e39e9a89a03d38a2ed`.
- Both retained HTML documents publish Family ID `48495278`, the title “Mobile device and optical
  imaging lens thereof”, Genius Electronic Optical ownership, the same two Chinese priority
  applications, six four-lens embodiment declarations, and FIGS. 1-28.
- Both current attempt-1 results contain one `parser_review_required` document item with
  `PatentParseError: embodiment f/Fno/HFOV line not found`; neither has a conversion request,
  worker receipt, prescription fingerprint, candidate, or ZMX.
- The two census items share layout
  `fd9effbaf6fa8c47e55c3c964acb452ea0355a4484d5fddc94108732b816fbb7` and have
  `table_count=0`; therefore the HTML text layer alone is not a valid prescription denominator.

## Completion evidence

The exact profile retains all six embodiments per root as source-faithful parser review. It
source-locks both raw/normalized official HTML identities, all 67 official page rasters, the
27/21-sheet denominator, FIGS. 1-28, all twelve optical/asphere pairs, FIG. 26, FIGS. 27-28, and the
official relationship facts. The raster audit is `4548cf92...aae83`; the independent OCR audit is
`27d5f1b8...b107`; production recovery is `db0e7d42...fb07`. FIG. 26 directly publishes all six
Fno values, correcting the generic error, but low-confidence/missing labels and coefficient tokens
remain and no cell is repaired or borrowed.

Both roots have append-only attempts 1-3; attempt 1 still matches HEAD and attempts 2/3 are
semantic-equal after excluding only `result_attempt`. The strict result set is
`eb1d0319...6076e` at 619/619, missing=0, corrupt=0. Generic census moves 157→155 roots/items; two
independent rebuilds are byte-identical at `1a112e9d...72b22`. All 400 offline patent tests pass.
One prior publication absent from the frozen cohort is durably queued.

Post-shovel measurement keeps generic first at 155 roots; every remaining exact-family grouping
has one root. Stable ordering selects Family ID `73978649`, root `US-12429633`, layout
`00fbf0bb21450b2de642f962a5eed0bc4e6cf3d1dd99f219dff4e0685932c189` next. Parent patent
saturation, formal intake, source crawl exhaustion, and external-family queues remain incomplete.
