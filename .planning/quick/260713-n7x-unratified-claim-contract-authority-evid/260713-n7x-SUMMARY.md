# Quick 260713-n7x summary — UNRATIFIED control plane

status: ACTIVE_UNRATIFIED_EXTERNAL_RELEASE_EVIDENCE_REQUIRED
goal_state: ACTIVE
north_star_gates: A=false,B=false,C=false,D=false,E=false,F=false
human_authority: unavailable
expert_metrics: unavailable
manufacturing_metrics: unavailable

## Outcome

This slice builds a fail-closed north-star governance and evidence control plane. It does not draw,
create, open, inspect or label a confirmatory sample/holdout and does not start CODE V. The claim,
acceptance contract, authority policy and canonical schema remain visibly `UNRATIFIED`; every
human-owned scope, threshold, rubric, TOR selection, signature, `[EXPERT]` value and GO field is null.

The gap ledger remains a non-authoritative index/cache. A–F can become true only by independent
recomputation from frozen contracts, external human signatures, sealed manifests, raw custody and
machine evidence, artifact hashes, Git/PR/CI and holdout logs. Loop2 observations remain exploratory:
P18 `29/21`, Stage B `8/8`, Stage C `2/46` and `6/48`, and one exact target are neither expert rates nor
manufacturing yield.

## Canonical schema state

This tracked summary defines structural invariants; it does not attest the commit/tree that contains
it, worktree cleanliness, review result, PR, CI, merge, or main-CI outcome. Those facts must be bound
by detached `RUN_CODE_RELEASE` or `EVIDENCE_RELEASE` objects after the reviewed tree is immutable.

The current author-side, non-gate `2994/2994` PowerShell observation is bound to the staged canonical
schema Git-blob SHA-256
`674e2e440c5ba671fe833308a12826dc3ab5a9ac0da916e7247665ab57db6c34`; the staged authority-policy
Git-blob SHA-256 is `485821144aa2bb2a11c6388da54d0048d3a5f8551220d96c95faefcfb5e41867`.
It covers JSON parse and
escaped-key duplicate rejection, the structural/selector/machine/release checks below, and the exact
expert-rater anchor → protocol tuple array → counted signature tuple closure. The GET PR-observation
leaf now uses an explicit digest-or-no-body-marker carrier and requires the one protocol marker. It is not fixed-tree
review or release evidence; any later canonical or authority write invalidates these two hashes.
The observation also covers registered closed-world inventory/admission objects, raw native
machine/OS evidence under externally anchored attestation controls, exact selected-policy schema and
control equality, activation reachability, the extended protected-run tuple, and pre-spawn plus
ticket/intent/start/terminal recomputation with fail-closed drift handling.
It additionally validates every non-null `reference_class` against the exact ten-member enum,
excludes the obsolete registered-object alias, proves the DURABLE_COMMIT subject has no back-edge to
the signed source-attestation record while the final receipt still binds both objects, and checks the
acceptance mirror against the canonical `27/27/48` binding counts.
It also constructs PRE_LEASE failure from the sole `INTENT_CONSUMED` pre-chain anchor at sequence
`-1` to the crash record as the first partial-chain member at sequence `0`, rejects every other
anchor sequence or claimed prior machine member, and verifies both STATE entry summaries list all
twelve rejected trees with the correct release-scope disposition. It also proves last-member selection
uses the existing exact-type intent path, one exact-type terminal-or-marker path, or one typed-
evidence/proof-or-marker path without a conditional object-type or reference-class union. Machine
partial-chain members independently split terminal-envelope and typed-leaf carriers, so
PROTECTED_TERMINAL replay never passes a registered object through a typed field; the obsolete mixed
`last_durable_member_hash` path is absent. Every one of the 24 nonterminal member kinds selects one
exact canonical template or signed-policy schema/domain and transition context; the normal and
recovery contexts for `POST_RUN_SNAPSHOT_AND_MONITOR` and `ZERO_STATE_PROOF` are disjoint and
exhaustive, and the schema expressly rejects same-class cross-kind leaf substitution.

The exact structural mirror is:

- 66 registered immutable object types, including canonical authority-roster, authority-quorum-rule,
  machine-entry-inventory and machine-admission-receipt content objects;
- 24 common-trust detached signer classes;
- ten closed hash-reference classes plus an exhaustive signed path registry covering hash, OID,
  SHA, marker and fingerprint fields;
- 26 sealed-sample-manifest fields;
- 19 selected machine-policy values;
- 27/27/48 ticket/durable-intent/terminal-receipt binding members and 33 ACTIVE-status-CAS bindings;
- 33/39-field initial inventory/admission templates, a 64-field durable pre-spawn receipt, a 36-field
  process-start receipt, five external OS-evidence source kinds and a 19-row machine time/status/OS/
  pre-spawn-gate external-anchor mapping;
- 32 evidence-bundle fields;
- 43 release-package and authority-mirror fields;
- 25 dynamically recomputed shared release/evidence bindings;
- 20 protocol bindings and 109 null human-owned choice positions;
- 29 machine typed-leaf templates, 64 release Git/CI typed templates, 35 machine lifecycle states,
  43 transition rows, 22 trusted machine event kinds and 10 crash-frontier variants.

The canonical authority objects make every stage roster/allowlist nonempty, every per-role and total
quorum minimum positive, and every accepted stage set dependent on at least one valid human
signature. Each counted identity/proof/key/allowlist tuple exactly matches one same-role registered
roster member; `AND` equals the roster tuple set and `OR` is only a nonempty duplicate-free subset.
The expert path requires a nonempty ordered rater tuple array whose bootstrap hash equals the
external anchor, `expert_rater_count >= 1` equal to that array length, and every counted signature
tuple to match one unique member of the same array while carrying the anchored hash. Thresholds must
lie in `(0, 1]`. At least one human-labelled candidate must have a signer set equal to the complete
protocol tuple array, with both dimensions recomputed under the signed combination rule, before D can
be true; incomplete or mismatched sets contribute zero to both expert ITT numerators. The
digest selector now derives 129 roots, 2,056 automatic paths, ten exact additions, three bootstrap
exclusions, and 2,063 nonbootstrap registry paths, including the inventory/admission and atomic
pre-spawn revalidation evidence fields,
the previously omitted lease-state hash
and two optional merge-commit OID-or-marker leaves.

The lease-state path is an explicit transition/accumulator root, not a typed-leaf digest. Barrier
post-state and per-operation terminal lease-state roots must be independently recomputed from the
complete raw native proof under the anchor-pinned authority-state accumulator policy; opaque signed-
root equality rejects.

New fail-closed boundaries include an externally anchored custody genesis and raw atomic durable
initialization receipt; independent store/clock/event-time attester allowlists; signed pre-draw and
review clocks with one unambiguous record-hash meaning; protected START/COMPLETE review timestamps;
cross-layer machine wrapper/payload/evidence/time equality; atomic ACTIVE and RELEASED status CAS;
durable PREPARED journal followed by one atomic OS-release-plus-OS_RELEASE_COMMITTED-journal
authority transaction; authority-atomic irrevocable `ABORTED_FINAL`-or-existing-`COMMITTED` restart
resolution; same-transaction global admission barrier plus lease insertion; and a final one-response GitHub Actions
repository closure proof that rejects reruns, higher attempts, mixed source profiles and fabricated
closure times. The pre-protocol O-07 chain additionally requires three distinct read-only review
scopes, an exhaustive inline raw-member byte set, one anchor-bound GitHub source profile and digest
registry, final PR-head review, terminal PR CI, expected-head CAS plus the independent provider
acquisition→snapshot→merge-admission→terminal base/policy freeze, and matching main/push CI. It binds
the accepted main merge commit separately from the reviewed PR-head commit and closes no A–F gate.

Inventory and admission attestations now include their `signature_algorithm` in the signed message,
use the fixed detached input, and bind external trust roots, the exact OS-evidence attester allowlist
and the anchored allowed algorithm set. Immediately before spawn, the durable receipt is the atomic
native revalidation/commit gate: raw bytes and parser membership derive the complete current inventory,
the verifier recomputes root/count, and the same transaction proves the current boundary/launcher/lease,
zero drift/uncovered/bypass, durable intent and consumed-ticket commit. Process start repeats the same
attestation record, single-use transaction, clock and valid-through counter; mutation, expiry, reuse or
a competing start rejects spawn and requires a new inventory/admission/activation chain.

## Dependency and release truth

Historical fixed commit `57c305f37da6a4cc511e485900e6dcb04602a988` / tree
`2b3c73d321677e863f2826e5e290e98e5b2bf8d7` and fixed commit
`a5ea60e0799c50af51110c2601169e5908a15851` / tree
`930767afdded9fb5419531643fc6b1e7f0352d82`, and fixed commit
`ff76ae0c8dd87533820b73725590134d0a05dd03` / tree
`431780535989ad8789de83ba0bbbdaec7e7da0ee`, and fixed commit
`d9e0e75ed291189bc3afbc8fc1f7f1ee05eb25fe` / tree
`00c7af0f45c09896708cd2c570f93f86bdf6746a`, and fixed commit
`bd2e1cf585375a3716a8ea2dd53f698b19068492` / tree
`cf9c6f3e36ad1e2cac8f899c407ddf3e54624950`, and fixed commit
`aca724155d464496e18e36700733931e9d05638a` / tree
`53c24554606312fcbe54f9ddd836142f524fb34e`, and fixed commit
`ead809c52b126cd9c9b99b14fd4db38cfcd22d2d` / tree
`b140543e34e7da9e725ce613ba72e09a7c8175d5`, and fixed commit
`8acb078317e08fde061bc33dcd226864c5b6dcea` / tree
`5856f8dd475942dcf3349d3302b22f0e3843e1aa`, and fixed commit
`0915ccf000438701bf10075e6f529ef349730e2a` / tree
`7e004a031844720fdeb0226a328f2c40cb4d0bb9`, and fixed commit
`2c74a540e11187d3fe8250e78d77dae291a7b7a7` / tree
`5784baccc296586863f2d2bcc2788719b3a2064c`, and fixed commit
`02f9d17cfeb2c34749612bdc41744a4820e537e7` / tree
`7abf1b6189718d2a9366bda030662181676fdb60`, and fixed commit
`ab7ce4d82876361e686a8da603fbbc6712c1aa7d` / tree
`f2ff988ab3bd0abe97dd066dd1a2aa90af820bcf` were all rejected and must not be published. The fourth
tree failed governance on empty-roster/zero-quorum and zero-rater/zero-threshold vacuity, and failed
machine review on three digest-selector omissions; its release-scope PASS was invalidated by those
same-tree findings. The fifth tree passed GOVERNANCE and MACHINE but failed RELEASE_GIT_CI because
its GET PR-observation leaf required a plain digest carrier where the transport rules require the
exact `GITHUB_HTTP_REQUEST_HAS_NO_BODY` marker; both same-tree PASS results are invalidated.
The sixth tree passed GOVERNANCE and RELEASE_GIT_CI but failed MACHINE because its activation-bound
inventory/admission references lacked closed-world typed objects, raw externally attested OS evidence,
selected-policy schema/control equality and propagation through the real spawn/receipt chain; both
same-tree PASS results are invalidated. The seventh tree failed GOVERNANCE because inventory/admission
attestation messages did not sign their `signature_algorithm` or explicitly bind external trust roots
and the allowed suite; it failed MACHINE because pre-spawn had no fresh raw native revalidation,
external attester record, single-use atomic transaction or process-start validity binding; its release
review also found the tracked six-versus-five count error.
The eighth tree failed GOVERNANCE because two content-hash rules used an unregistered hash-class
alias and failed MACHINE because its DURABLE_COMMIT event subject closed a cycle through the source-
attestation record and because the acceptance mirror retained contradictory machine-binding counts;
its same-tree release PASS is invalidated.
The ninth tree failed MACHINE because PRE_LEASE required a last member that was not in the partial-
chain member set while defining no pre-chain sequence sentinel, and failed GOVERNANCE/RELEASE because
the two STATE entry summaries omitted the eighth rejected tree.
The tenth tree failed MACHINE because one `last_durable_member_hash` path selected a registered
intent-envelope object for PRE_LEASE and typed evidence leaves for other frontiers even though the
registry permits one reference class per fixed path; its GOVERNANCE and RELEASE passes are invalidated.
The eleventh tree failed GOVERNANCE/MACHINE because its partial-chain `typed_leaf_hash` still mixed
typed leaves with the registered protected-terminal envelope and because one crash registered-object
path conditionally selected two exact object types. It also failed MACHINE/RELEASE because tracked
PLAN/SUMMARY/VERIFICATION required `2054/2061` while the fixed schema recomputed `2055/2062`.
The twelfth tree passed GOVERNANCE and RELEASE_GIT_CI but failed MACHINE because only eleven
recovery member kinds had an exact kind-to-template resolution; thirteen ordinary typed kinds were
unmapped, permitting a same-class cross-kind leaf such as a lease-acquisition receipt to be claimed
as `PRE_LAUNCH_SNAPSHOT`. Both same-tree PASS results are invalidated.
No text in this tracked summary can prove that a later tree passed review or release gates.

The finite execution DAG is:

`X-00A exact schema/anchor/goal freeze → X-00B run-code-independent active minimum-claim floor/checkpoint`; in parallel, `O/M offline implementation → O-07 fixed run-code release`; those chains converge at final H-01 protocol and H-02 TOR signatures → H-03 custody/draw/activation → R-00 complete machine population and prelabel freeze → R-01 optical/repeat/TOR/manufacturing closure → R-02 protected human labels → R-03 independent raw A–E recomputation → O-09 final evidence release → H-04 human GO.

Before X-00A/X-00B, only O/M and other non-holdout, non-threshold offline work may proceed. After
X-00B, the floor may be active, but protocol/TOR, machine execution and holdout remain blocked until
O-07/H-01/H-02/H-03 close. The
normal machine terminal path is receipt-last and lease-held through:

`terminal artifacts → post-run monitor → zero-state proof → ACTIVE status CAS → machine receipt → protected terminal → release transition → PREPARED journal → OS release + OS_RELEASE_COMMITTED journal as one atomic authority transaction → RELEASED status CAS → durable release receipt`.

No real-machine command is authorized by this slice.

## Files

- `.planning/north-star/preregistration-manifest-schema-unratified-v0.1.json`
- `.planning/north-star/claim-envelope-unratified-v0.1.md`
- `.planning/north-star/acceptance-contract-unratified-v0.1.md`
- `.planning/north-star/authority-policy-unratified-v0.1.json`
- `.planning/north-star/gap-ledger.json`
- `.planning/north-star/evidence-matrix.md`
- `.planning/north-star/need-human-ratification-v0.1.md`
- `.planning/north-star/backlog.md`
- `.planning/STATE.md`
- `.planning/decisions.log`
- this summary, `260713-n7x-PLAN.md` and `260713-n7x-VERIFICATION.md`

## Remaining gates

For any future publication, one immutable commit/tree must pass the complete static suite and three
entirely fresh, pairwise-distinct non-author read-only reviews covering `GOVERNANCE`, `MACHINE`, and
`RELEASE_GIT_CI`; PR CI, accepted expected-head CAS, the independent provider
acquisition→snapshot→merge-admission→terminal freeze, and matching main/push CI must then be reconstructed
from raw detached evidence. A pre-run O-07 control-plane release still cannot satisfy north-star F;
only the later post-A–E O-09 package can close F. Human ratification, sealed holdout execution, expert
labels, TOR disposition, A–E evidence, F-release evidence and final human GO remain unavailable.
