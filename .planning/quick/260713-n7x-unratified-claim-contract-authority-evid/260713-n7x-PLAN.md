---
quick_id: 260713-n7x
status: control_plane_released_unratified
mode: quick-full
validation: required
owner: Codex
base_commit: d35b3d07cead830396d24d2b10665199c73985e0
base_tree: 5121336ca69bb0397de244550114aa3d8487498c
must_haves:
  truths:
    - "North-star gates A-F are all false; expert and manufacturing metrics without authorized human signatures remain unavailable."
    - "All loop2 and otherwise observed evidence is permanently exploratory and cannot be promoted to confirmatory by this task."
    - "The canonical schema, every contract/claim/authority mirror, and every signature template produced here is visibly UNRATIFIED and cannot validate itself."
    - "The ledger is an index/cache only; gate truth must be recomputed from frozen contracts, human signatures, sealed manifests, machine receipts, artifact hashes, Git/PR/CI, and holdout access logs."
    - "This task does not draw, open, inspect, run, label, or create a confirmatory sample or holdout, and does not start CODE V."
    - "Gate D can count only expert signatures whose exact identity/proof/key tuple is in the nonempty signed protocol rater array whose bootstrap hash equals the external anchor; a candidate numerator requires the complete distinct array and signed combination rule."
  artifacts:
    - path: ".planning/north-star/preregistration-manifest-schema-unratified-v0.1.json"
      provides: "Single closed-world acyclic schema registry with out-of-band schema/governance anchors, exact mapping/eligibility contents, pre-label candidate freeze, protected-run machine links, evidence/release, and detached final decision"
    - path: ".planning/north-star/claim-envelope-unratified-v0.1.md"
      provides: "Non-authoritative mirror of the human-signed active minimum-claim floor and protocol claim, with no AI signature or scope ratification"
    - path: ".planning/north-star/acceptance-contract-unratified-v0.1.md"
      provides: "Frozen-protocol template covering units, ITT, endpoints, exclusions, repeat, holdout, review time, and productivity"
    - path: ".planning/north-star/authority-policy-unratified-v0.1.json"
      provides: "Fail-closed allowlist/AND-OR/signature binding template with every approval unset"
    - path: ".planning/north-star/gap-ledger.json"
      provides: "ACTIVE gap index with A-F false/unavailable and exploratory-only evidence references"
    - path: ".planning/north-star/evidence-matrix.md"
      provides: "Per-gate raw evidence requirements and present truth"
    - path: ".planning/north-star/need-human-ratification-v0.1.md"
      provides: "Concrete NEED package with decision owners, options, recommendation, no-response effect, and signature fields"
    - path: ".planning/north-star/backlog.md"
      provides: "A-F mapped staged backlog with dependencies, exit conditions, machine checks, and retry budgets"
  key_links:
    - from: ".planning/north-star/claim-envelope-unratified-v0.1.md, .planning/north-star/acceptance-contract-unratified-v0.1.md, and .planning/north-star/need-human-ratification-v0.1.md"
      to: ".planning/north-star/preregistration-manifest-schema-unratified-v0.1.json"
      via: "non-authoritative mirrors only; the helpers are never independently signed"
    - from: ".planning/north-star/gap-ledger.json"
      to: ".planning/north-star/evidence-matrix.md"
      via: "gate IDs A-F and raw-evidence references"
    - from: ".planning/north-star/acceptance-contract-unratified-v0.1.md"
      to: ".planning/north-star/claim-envelope-unratified-v0.1.md"
      via: "required claim-envelope hash binding, left unset until human ratification"
    - from: ".planning/north-star/need-human-ratification-v0.1.md"
      to: ".planning/north-star/authority-policy-unratified-v0.1.json"
      via: "the exact humans/roles and signatures needed before confirmatory work"
    - from: ".planning/north-star/authority-policy-unratified-v0.1.json"
      to: ".planning/north-star/acceptance-contract-unratified-v0.1.md and .planning/north-star/claim-envelope-unratified-v0.1.md"
      via: "role-specific allowlists, AND/OR semantics, external identity, signature time, and content-hash bindings"
    - from: ".planning/north-star/evidence-matrix.md"
      to: "frozen contracts, human signatures, sealed manifests, machine receipts, artifact hashes, Git/PR/CI, and holdout access logs"
      via: "each A-F row names the raw evidence classes required for recomputation and never cites the ledger as proof"
    - from: ".planning/north-star/backlog.md"
      to: ".planning/north-star/gap-ledger.json"
      via: "each work item maps to one or more currently false A-F gates"
---

# Quick Task 260713-n7x: establish the UNRATIFIED north-star control plane

## Goal

Create the first tracked, fail-closed north-star governance and evidence control plane from
the clean `origin/main@d35b3d0` baseline. The task prepares material that authorized humans
can ratify later, records present gaps without promoting exploratory evidence, and freezes a
finite offline backlog. It does not make any north-star gate true.

Historical fixed commit `57c305f37da6a4cc511e485900e6dcb04602a988` / tree
`2b3c73d321677e863f2826e5e290e98e5b2bf8d7` and fixed commit
`a5ea60e0799c50af51110c2601169e5908a15851` / tree
`930767afdded9fb5419531643fc6b1e7f0352d82`, and fixed commit
`ff76ae0c8dd87533820b73725590134d0a05dd03` / tree
`431780535989ad8789de83ba0bbbdaec7e7da0ee`, and fixed commit
`d9e0e75ed291189bc3afbc8fc1f7f1ee05eb25fe` / tree
`00c7af0f45c09896708cd2c570f93f86bdf6746a`, and fixed commit
`bd2e1cf585375a3716a8ea2dd53f698b19068492` / tree
`cf9c6f3e36ad1e2cac8f899c407ddf3e54624950`, and fixed commit
`aca724155d464496e18e36700733931e9d05638a` / tree
`53c24554606312fcbe54f9ddd836142f524fb34e`, and fixed commit
`ead809c52b126cd9c9b99b14fd4db38cfcd22d2d` / tree
`b140543e34e7da9e725ce613ba72e09a7c8175d5`, and fixed commit
`8acb078317e08fde061bc33dcd226864c5b6dcea` / tree
`5856f8dd475942dcf3349d3302b22f0e3843e1aa`, and fixed commit
`0915ccf000438701bf10075e6f529ef349730e2a` / tree
`7e004a031844720fdeb0226a328f2c40cb4d0bb9`, and fixed commit
`2c74a540e11187d3fe8250e78d77dae291a7b7a7` / tree
`5784baccc296586863f2d2bcc2788719b3a2064c`, and fixed commit
`02f9d17cfeb2c34749612bdc41744a4820e537e7` / tree
`7abf1b6189718d2a9366bda030662181676fdb60`, and fixed commit
`ab7ce4d82876361e686a8da603fbbc6712c1aa7d` / tree
`f2ff988ab3bd0abe97dd066dd1a2aa90af820bcf` were rejected by independent read-only reviews and must
not be published. This tracked plan cannot attest its containing tree or any later review/PR/CI;
the canonical O-07/O-09 detached release objects are the only accepted publication evidence.
The fourth review found empty-roster/zero-quorum vacuous authorization, zero-rater/zero-threshold
expert closure, and three runtime digest-selector omissions. The fifth review found the GET
PR-observation request-body carrier impossible to satisfy because a plain digest and the exact no-body
marker were required simultaneously. The sixth review found that inventory/admission hashes were not
backed by closed-world typed objects, raw externally attested OS evidence, selected-policy equality,
or the actual spawn/receipt chain. Both same-tree PASS scopes are invalidated and this MACHINE P1 is a
mandatory fix-forward input. The seventh review then found that inventory/admission signed messages
excluded their signature algorithms and did not bind an external allowed suite/trust-root contract,
while durable pre-spawn still lacked fresh raw native revalidation, parser membership, an external OS
attester record, a single-use atomic gate transaction and a validity bound repeated by process start;
STATE also miscounted six trees as five. All three fixed-tree reviews were restarted on the eighth tree.
The eighth review then found two uses of an unregistered hash-class alias, an unconstructible
DURABLE_COMMIT time-source/attestation-record hash cycle, and contradictory `25/25/46` versus
`27/27/48` machine-binding counts in the acceptance mirror. All three fixed-tree reviews were
restarted on the ninth clean-parent tree. The ninth review found stale STATE entry summaries and an
unconstructible PRE_LEASE crash branch with no pre-chain sequence/member anchor. All three reviews
were restarted on the tenth clean-parent tree. The tenth review found that the locally replayable
anchor still overloaded one fixed hash path across registered-object and typed-leaf reference classes.
The eleventh review found the same cross-class union in `machine_partial_chain_member_template`, a
second conditional union of intent and terminal exact object types in one crash registered-object
path, and stale `2054/2061` selector requirements against the fixed schema's `2055/2062` result.
The twelfth review found that its exact kind-to-template resolution covered only eleven recovery
member kinds while thirteen ordinary typed kinds remained unmapped. That gap permitted same-class
cross-kind substitution, including a real lease-acquisition receipt claimed as
`PRE_LAUNCH_SNAPSHOT`; both same-tree PASS scopes are invalidated. The thirteenth clean-parent
fix-forward defines one exhaustive 24-kind exact canonical-template or signed-policy schema/domain
resolution map, including disjoint normal/recovery contexts for `POST_RUN_SNAPSHOT_AND_MONITOR` and
`ZERO_STATE_PROOF`. All three reviews must restart on the thirteenth clean-parent tree.

## Guardrails

- Never create, infer, import, select, or sign an `[EXPERT]`, good-part, production-usable,
  yield, ratified/final claim-envelope, final threshold, or GO decision. Creating an explicitly
  `UNRATIFIED` template whose human-owned fields are all null is allowed by this task.
- Never treat an AI-authored Git commit, silence, or a self-declared reviewer string as a
  human signature.
- Never draw or access a confirmatory sample or sealed holdout in this task.
- Never run Python, `uv`, pytest, an old runner, CODE V, or `codevm` for this documentation
  task. Static PowerShell/`git` checks only.
- Preserve all old runtime artifacts. Refer to them only as exploratory reusable components
  or incident evidence.
- Do not modify scoring rulers, redline files, global configuration, old dirty worktrees, or
  runtime evidence.

## Task 1 — Draft the human-ratifiable contract surface

**Files**

- `.planning/north-star/claim-envelope-unratified-v0.1.md`
- `.planning/north-star/acceptance-contract-unratified-v0.1.md`
- `.planning/north-star/authority-policy-unratified-v0.1.json`
- `.planning/north-star/preregistration-manifest-schema-unratified-v0.1.json`

**Action**

Write one explicitly `UNRATIFIED` closed-world canonical schema plus non-authoritative mirrors for
the signed active minimum-claim floor, acceptance/statistics contract, authority policy, and NEED response.
The canonical registry begins with a goal-bound governance anchor, a four-object minimum-claim
message/signature chain and an external durable high-water checkpoint. The external verifier supplies the expected active floor, validates its
human authority signature set, replays its genesis/successor lineage, and accepts only an
equal-or-broader successor or protocol claim; narrower or unverifiable claims remain exploratory.
Cover claim, optical-expert, manufacturing/statistics, machine-execution, and final-GO authorities
with explicit stage-specific roster/quorum/AND-OR semantics; whether 主公 is an optical
expert; immutable identity allowlists; contract/claim,
code commit+tree, evidence-schema, sampling frame, external trust policy/root set, timestamp,
external-source, and signature bindings. The actual sealed-manifest hash is forbidden before the
custodian draw. Every human-owned Markdown mirror field must remain null; explanatory prose must not
contain an alternative selected value. The claim template
must expose human-owned final choices for supported scenarios and
specification ranges, representative target distribution, minimum independent target/seed-cluster/
holdout counts, patent-family independence, non-waivable endpoints, and excluded external-tool
walls. Freeze all protocol fields required by the Goal: target/cluster/patent-family/candidate/
attempt/run IDs, eligibility timing, planned candidate slots, planned run units, mutually exclusive
terminal states, retry mapping, the complete ITT mapping for blocked/degraded/failed/non-converged/
  missing/unlabelled/undelivered/saturated/compensation-failed/contaminated units; exclusions that
  additionally require independent raw evidence proving immutable input invalid and a frozen
  automatic rule; separate run-unit delivery and candidate-slot expert denominators with exact
  numerator/denominator/exclusion/cluster/CI reporting; candidate content/lineage fingerprints,
  equivalence/duplicate clusters, all-slot denominators, and at-most-one independent numerator per
  cluster. Freeze actual candidate bytes, required human view objects, lineage and equivalence
  clusters once in a complete pre-label manifest before the first label intent; a label may only
  mirror the resolved member and cannot declare its own cluster. The signed protocol also freezes
  the human-selected required-label-view rule. Cover the two independent expert labels; exact TOR semantics, units, table-hash selection,
  Monte Carlo denominator, compensator, aggregation, saturation mapping and compensation-failure
  mapping; confidence/cluster method; repeat/holdout,
expert review time, productivity baseline, and exit-code table. Specify holdout custodian
separation, out-of-workspace encryption/ACL, access audit, protocol signatures before draw,
  draw unsigned content → signature message → detached signature record → envelope, authority
  activation plus signature set before access, and
  membership-proved one-time capabilities with signed intent/terminal envelopes that advance both
  audit and consumed-index roots for unseal/view/run/label/export; specify the rule that a
  protected run intent is in a complete per-attempt bijection with its launch ticket, durable
  machine intent, terminal receipt and protected terminal, while a crash remains
  `CONSUMED_INCOMPLETE`. Make planned identity and eligibility commitments exact domain-separated
  nested contents with deterministic ordinal ordering, and make draw/manifest/activation values
  byte-equal within one batch. Bind the final schema hash to a bootstrap verifier recomputation and
  the external anchor, protocol, sealed manifest and activation objects; specify the rule that a
substantive contract/code/rubric/eligibility/denominator/threshold/analysis change invalidates the
  affected confirmatory batch and requires a new sealed holdout. Define separate immutable objects
  and an acyclic out-of-band schema/governance anchor → protocol → approval messages/records/signature set →
  exact mapping/eligibility contents → canonical sealed manifest/draw envelope → pre-activation
  capability root → activation approval messages/records/signature set → membership proof →
  candidate pre-label freeze/protected-access envelopes → protected-run machine link set → evidence
  bundle → `F_release_evidence_complete` release-evidence package → detached final statement/
  signature-message/signature-record/signature-set/outer-receipt DAG. Bind protocol, activation, custodian, broker,
  expert and final-GO identity through one externally pretrusted root policy and exclude release/
  final objects from the evidence bundle.
  Promote ITT population membership, invalid-input exclusion, and duplicate-cluster treatment to
  canonical non-overridable hard invariants. Leave all approval/signature/hash values and every human-owned final scope, sample-size,
  threshold, rubric/TOR, and GO selection explicitly `null`/`UNRATIFIED`. Quantified options may
  appear only in the NEED package; no reply must ever select the recommendation. Every non-passing
  planned run or candidate slot remains in its own frozen ITT denominator and counts as not passed.
  Any conditional-on-delivered rate is diagnostic only and can never satisfy a north-star gate. No
  downstream batch may call these templates ratified.

**Verify**

- All three JSON files parse with PowerShell `ConvertFrom-Json`; explicit
  property assertions prove schema/policy status is exactly `UNRATIFIED`, every protocol/final
  approval/signature is null, and all human-owned final choice fields remain null.
- Registry/property assertions prove all 66 object types map one-to-one to unique templates/domain
  tags and minimum-claim/protocol/activation/prelabel-freeze/final signature sets are explicit
  objects. The two authority roster/quorum types are registered content objects; stage rosters and
  allowlists are nonempty, per-role/total quorum minima are positive, within-role `AND`/`OR` is exact,
  every counted identity/proof/key/allowlist tuple exactly matches one same-role roster member,
  `AND` equals the roster tuple set, `OR` is a nonempty duplicate-free subset, and every accepted
  stage set contains at least one valid human signature. All 24
  signature classes use the single common detached-signature input formula over unsigned
  message → detached signature record → envelope/set; every object hash is
  external to its own preimage; capability membership, literal nonce consumption, chained audit/
  consumed-index roots, and evidence/release final-root equality are mandatory.
- Exact assertions require the common trust coverage set to include minimum-claim, protocol,
  activation, draw custodian, prelabel-freeze broker, intent broker, terminal broker, custody store
  and control-source attesters, pre-draw clock attester, review clock/event-time/store attesters,
  machine-status store attester, release reviewer, post-merge attester, expert, and final-GO;
  require all eight TOR fields;
  require the selected machine object to have the canonical 19 typed closed-world values, exact
  ordered three phases/eight subjects, separate broker-held lease,
  and `terminal_artifacts → post_run_snapshot_and_monitor → zero_state_proof → ACTIVE status CAS →
  machine receipt → protected terminal → release transition → PREPARED journal → OS release +
  OS_RELEASE_COMMITTED journal as one atomic transactional-authority operation → RELEASED status CAS
  → durable release receipt`.
- Machine assertions additionally require exact 27/27/48 launch-ticket/durable-intent/terminal-
  receipt binding sets, a 33-binding ACTIVE-status CAS, closed status/FSM/crash replay, and a complete
  forward-and-reverse protected-run/machine chain. The transactional lease/journal authority uses its
  own externally anchored native receipt-attester policy (not a nineteenth common signer class),
  irrevocable PREPARED→ABORTED_FINAL-or-COMMITTED resolution, and a same-transaction global admission
  barrier plus lease insertion.
- Inventory/admission assertions require two registered closed-world objects, complete raw native
  machine/OS captures under the external OS-evidence attestation controls, verifier-recomputed schema
  hashes equal to the selected policies, activation equality and propagation through the extended
  protected-run tuple and every actual spawn/receipt stage. Drift or boundary change requires a new
  inventory/admission/activation chain before spawn.
- Barrier post-state and per-operation terminal lease-state roots use the explicit accumulator-root
  class and independently recompute from the complete raw native proof under the anchor-pinned
  authority-state accumulator policy; typed-leaf classification or opaque root equality rejects.
- Release assertions require the exact 32-field evidence bundle, 43-field release package and
  authority mirror, dynamic 25-field intersection, 15-field PR aggregate, 20-field main-run member,
  15-field main-CI aggregate and 64 release typed templates; ten exhaustive digest classes, eight
  terminal-pagination null paths, fifteen source-profile-bound GitHub leaves, one inline raw-member
  byte set, and three distinct review scopes; a final reviewed PR head commit/tree, compare-and-swap
  merge against that head,
  terminal-success required PR checks, and exact nonempty post-merge `push` runs on
  `refs/heads/main` using reviewed workflow blobs and the merge SHA, followed by one bounded
  repository-wide Actions closure response with exact raw capture times/Date and anti-rerun proof.
- Digest-registry assertions mechanically derive 129 runtime template roots, 2,056 automatically
  selected paths, ten exact additional paths, three bootstrap exclusions and 2,063 unique
  nonbootstrap registry paths. They explicitly cover all inventory/admission evidence fields, the
  lease-state post-terminal-resolution hash
  and both optional merge-commit OID-or-protocol-marker leaves.
- Expert assertions require a nonempty rater allowlist, integer `expert_rater_count >= 1`, both
  expert-rate thresholds in `(0, 1]`, and at least one valid human expert-label envelope with both
  dimensions before D can be true; zero-rater/zero-threshold vacuity rejects.
- Negative assertions reject opaque/reordered/gapped mapping contents, eligibility-rule swaps,
  canonical-schema hash splices, cross-batch draw→activation reuse, incomplete/second pre-label
  freezes, post-freeze candidate mutation, same-byte or same-semantic candidates split across
  clusters, producer-selected partitions, wrong candidate bytes or view objects, prior unclosed
  exposure, suggestion-bearing blank forms, label signatures before required access/raw human entry,
  run-ticket/intent/receipt/terminal splicing and orphan links, and main-CI runs with wrong
  event/ref/workflow blob/SHA or pre-merge timestamps.
- `Select-String -SimpleMatch` checks all four authority roles, stage-specific AND/OR, all required hash bindings, both
  expert labels, ITT, holdout custody/audit/unseal, TOR, review time, productivity baseline, and
  confirmatory invalidation rule. The checks only inspect bytes; they never execute document text.
- Anchored read-only regex assertions require every human-owned scope, sample-size, threshold,
  rubric/TOR, GO, approval, signature, and hash-selection field in the claim/contract/NEED Markdown
  files to have exactly one `field_name: null` line; duplicate anchors or any non-null form fail.
- Only `ConvertFrom-Json`, property access, `Select-String -SimpleMatch`, and `git diff --check`
  plus the anchored `Select-String -Pattern` null checks are permitted; `Invoke-Expression` and
  equivalent evaluation are forbidden.

**Done**

All required decisions are concrete enough for a human to sign, while the artifacts remain
machine-obviously invalid for confirmatory use until verifiable external signatures exist.

## Task 2 — Record present A–F truth and issue the NEED package

**Files**

- `.planning/north-star/gap-ledger.json`
- `.planning/north-star/evidence-matrix.md`
- `.planning/north-star/need-human-ratification-v0.1.md`
- `.planning/decisions.log`

**Action**

Record internal state `ACTIVE`; contract/claim version `v0.1-draft`; A–F as false, with expert
and manufacturing metrics unavailable. Index only verified existing paths/hashes and label all
observed loop2 evidence `exploratory_reusable_component` or `incident_evidence`; never copy a
ledger assertion into a gate result. In the evidence matrix, distinguish required raw evidence
from currently available evidence for A–F. In the NEED package identify who must decide, why AI
lacks authority, the exact disk/Git evidence, 2–3 quantified trade-off options, a recommendation,
the effect of no response, and empty signature fields. Append one decision-log entry for the
fail-closed north-star reset and GSD entry; do not rewrite history.

**Verify**

- Both JSON files parse with `ConvertFrom-Json`.
- PowerShell property assertions prove the ledger gate-key set is exactly `A,B,C,D,E,F`, every
  gate value is false, expert/manufacturing outputs are unavailable, and contract/claim status is
  `UNRATIFIED`.
- Property assertions prove the ledger declares itself non-authoritative, every loop2 evidence
  reference is classified only as `exploratory_reusable_component` or `incident_evidence`, and
  each indexed fact links to a raw source or is explicitly unavailable.
- P18 `29/21`, Stage B `8/8`, Stage C `2/46` and `6/48`, and the single exact target are never
  presented as expert rates or manufacturing yield.

**Done**

A fresh reader can recompute that the north star is not achieved, identify every missing raw
evidence class, and hand the exact ratification choices to the authorized humans without any
AI-created approval.

## Task 3 — Freeze the staged backlog and validate the documentation slice

**Files**

- `.planning/north-star/backlog.md`
- `.planning/quick/260713-n7x-unratified-claim-contract-authority-evid/260713-n7x-SUMMARY.md`
- `.planning/quick/260713-n7x-unratified-claim-contract-authority-evid/260713-n7x-VERIFICATION.md`
- `.planning/STATE.md`

**Action**

Build a dependency-ordered A–F backlog. Non-binding human discussion (`H-00`) may run in parallel,
and the implementation-independent minimum-claim floor/signature/checkpoint may be established
before the code release. Final protocol/TOR signatures cannot precede the fixed O/M/O-06/O-08 code
release (`O-07` with review, PR checks, expected-head CAS, independent provider
acquisition→snapshot→merge-admission→terminal base/policy freeze, and matching main-CI success); they must bind that
release plus the verified active floor, and draw/activation (`H-03`) must follow those final
protocol/TOR signatures. First code slice is a strict preregistration/ITT protocol
kernel; subsequent offline slices cover C1 EFL+IMH derived-FOV exit semantics, per-requirement
artifact namespaces, multi-target exports/reproduction, evidence diagnostics, signed two-dimension
expert-label import, and review/PR/CI tree binding. Separately place the canonical CODE V launcher,
machine-wide OS lease, launch ticket, bypass closure, conflict monitoring, and receipt-last work
ahead of every future real-machine run. Give every item dependencies, exit criteria, exact offline
  machine checks, and a finite `1 implementation + at most 2 evidence-driven fix-forward` budget.
  Split pre-run code release (O-07) from post-confirmatory evidence/handoff release (O-09). The
  confirmatory lane is explicitly H-03 custody/draw/activation → R-00 full machine population and
  prelabel freeze → R-01 optical/repeat/TOR/manufacturing closure → R-02 protected human labels →
  R-03 independent raw A–E recomputation → O-09. O-09 closes only
  `F_release_evidence_complete`; H-04's separately verified final signature set and GO complete the
  Goal without changing F.
Mark human ratification, TOR, expert labels, holdout custody/unseal, and final GO as external gates,
without blocking safe offline work. Finish every tracked artifact, including the GSD summary,
verification, `STATE.md` quick-task row, and decisions entry, before the final static validation.
Commit that complete tree, then obtain three pairwise-distinct non-author read-only reviews covering
`GOVERNANCE`, `MACHINE`, and `RELEASE_GIT_CI`, bound to the exact commit/tree through detached review
objects. Do not add a tracked review artifact after the tree is frozen. Any finding or any subsequent
tracked write creates a new commit/tree and requires the static checks and all three reviews again.

**Verify**

- Every backlog item maps to A–F, declares dependencies/exit criteria/checks/retry budget, and
  never asks for an unbounded retry.
- CODE V work is ordered before any real-machine task and no command in this task launches it.
- M-03/M-05/M-06 use the same exact 19-key machine policy, ordered pre/during/post snapshot subjects,
  broker-held lease, and exact `release transition → PREPARED journal → OS release +
  OS_RELEASE_COMMITTED journal as one atomic authority transaction → RELEASED status CAS → durable
  release receipt` order plus the schema's fail-closed zero-state condition.
- `git diff --check` passes.
- After all tracked writes and static checks, three fresh independent read-only reviewers check the
  same fixed commit/tree SHA and exact tracked review-scope manifest. Any finding is fixed by the
  author; the new commit/tree is checked and all reviews restart before PR publication.

**Done**

Done means one immutable tree is internally consistent, statically clean, and has detached PASS
evidence for all three review scopes plus PR CI, accepted expected-head CAS, the independent provider
acquisition→snapshot→merge-admission→terminal freeze, and matching main/push CI.
This tracked plan does not claim that condition. The Goal remains `ACTIVE`; no A–F gate, human
signature, or confirmatory evidence is claimed complete.
