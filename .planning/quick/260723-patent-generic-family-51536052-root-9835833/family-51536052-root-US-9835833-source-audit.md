# Family 51536052 / US-9835833 source audit

## Frozen publication and lineage

This shovel is source-locked to `US-9835833-B2` (root `US-9835833`,
application `14/848945`, Family `51536052`). The retained official HTML has
54,311 bytes and SHA-256
`9b1038d3ad24d26282a720e3e9e0ee07fdae49d2e49e850ce9b004cacfdb419d`;
its normalized text has 41,159 characters and SHA-256
`9db69ec2ce098224060a0734f44bdfc9cc27ee7db85a194bbce0b0e17a7cf8a0`.
The B2 identifies itself as a continuation of `PCT/JP2013/007647`, filed
2013-12-26, and claims Japanese priority `2013-052925` from 2013-03-15. It
also identifies prior publication `US-20150378136-A1`. No value, boundary or
outcome is imported from any parent, priority filing or prior publication.

The exact bibliography, raw/normalized section and paragraph hashes, claims,
figures, inline formulas and all ten flattened TABLE-US payloads are frozen in
`family-51536052-root-US-9835833-denominator.json`. Background/Summary
paragraphs `(1)-(25)` and Description paragraphs `(1)-(62)` are consecutive;
the separate cross-reference paragraph preceding BACKGROUND is not a duplicate
source item. Claims are exactly 1-9, with only claim 1 independent. The brief
description declares 17 panels: FIGS. 1-3, 4A-4D, 5A-5D, 6A-6D and 7A-7B.
The HTML contains 26 ordered figure references, 18 paired inline-formula
processing instructions, zero MathML objects, zero native tables and zero
embedded images.

## Exact item and table denominator

The exact denominator is four source items:

1. Example 1, Description `(32)-(44)`, FIGS. 1 and 4A-4D, TABLES 1-3 plus
   shared TABLE 10.
2. Example 2, Description `(45)-(49)`, FIGS. 2 and 5A-5D, TABLES 4-6 plus
   shared TABLE 10.
3. Example 3, Description `(50)-(57)`, FIGS. 3 and 6A-6D, TABLES 7-9 plus
   shared TABLE 10.
4. The mirrorless-camera imaging-apparatus wrapper, Description `(58)-(61)`,
   FIGS. 7A-7B and claim 9.

The generic embodiment described before the numerical examples is explicitly
common with Example 1 and is not a fourth prescription. TABLE 10 compares
conditional-formula values for Examples 1-3 and is shared evidence, not a new
design. Each numerical example has one ordered lens-data table, one direct
system-metadata table and one asphere-coefficient table. TABLES 2/5/8 directly
publish respectively `f=12.37`, back focus `1.00`, F-number `2.89/2.89/2.88`
and full field `100.34/98.82/101.26` degrees.

## Lossless conversion boundary

Description `(38)-(39)` defines the asphere polynomial with coefficients `KA`
and `Am`, where `m=3, 4, ... 20` and `Am` multiplies `h^m`. TABLES 3/6/9 each
publish the complete `KA,A3,...,A20` row sequence. Every odd row
`A3,A5,A7,A9,A11,A13,A15,A17,A19` is nonzero on every listed aspherical
surface: surfaces 3/17/18 for Example 1, 3/8/18/19 for Example 2, and 3/19/20
for Example 3.

The current `PatentSurface`/CODE V `XASPHERE` contract maps even radial powers
only. Dropping or approximating any directly published odd term would lose
source truth. All three prescriptions therefore remain precise nonterminal
`parser_review_required.deterministic_parser_rejected` items. The camera
wrapper adds a body, mount, sensor, controls, signal processing and recording
medium around the disclosed lens, but no independent optical prescription; it
closes as `confirmed_no_prescription.imaging_apparatus_wrapper_only`. The root
is consequently `mixed_nonterminal`. No worker request, conversion receipt,
fingerprint, ZMX, formal intake or CODE V call is allowed.

## PDF/raster scope

The retained machine-readable B2 fully settles every category, item, table,
claim, section, figure and formula boundary needed for this classification.
No PDF or raster review was required or used; no OCR, enhancement, repair,
measurement, numeric raster transcription, derivation or inference occurred.
