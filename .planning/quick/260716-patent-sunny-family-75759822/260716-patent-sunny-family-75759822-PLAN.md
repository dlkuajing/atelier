# Quick: Patent Sunny Family 75759822

**Status:** complete-shovel-saturation-incomplete
**Parent:** patent saturation engineering (active / incomplete)
**Started:** 2026-07-16
**Entry evidence:** strict 619/619 replay result set
`450f7be019a47b6492afd0fc0849b2036d1f5071a452b386453cc3b76c47259f`, with
`sunny_embodiment_metadata_missing` at 187 items / 51 roots and no corrupt result.

## Objective

Resolve the largest exact Sunny family cluster, Family ID `75759822`: `US-12216247` and
`US-20220244497`. Reconcile every disclosed embodiment, surface/asphere table, system-metadata
binding, field definition, application/publication relationship, and required coordinate rule.
No family/title/layout similarity may supply a missing value, and no derived field is allowed
unless the exact source defines the transform and publishes every operand for that embodiment.

## Plan

1. Freeze the current 187-item Sunny census and source-lock both official HTML publications,
   their application relationship, complete table/embodiment denominator, and current ten-item
   failure set.
2. Audit all prescription and metadata blocks, source equations, drawings, material conventions,
   folded/decentered coordinates, and any image-only data before choosing parsing or a terminal.
3. Implement only exact-source fail-closed behavior. Preserve every absent, ambiguous, damaged,
   or unsupported value as a terminal or parser review; never infer or repair optical values.
4. Replay both roots append-only twice, compare semantics with only permitted retry identity
   excluded, audit all 619 roots, and rebuild the after Sunny census twice.
5. Run parser/replay/census/saturation/process/guard tests, Ruff, and `git diff --check` with
   `PYTHONUTF8=1`, active offline guard, and CODE V inventory zero.
6. Update STATE, decisions, this plan, and the recovery cursor with exact hashes. Commit this
   shovel, then remeasure all executable buckets. Parent saturation remains incomplete.

## Completion evidence

- `family-75759822-source-audit.md` binds the exact B2/A1 application relationship, raw and
  normalized source hashes, five embodiments, five surface/asphere table pairs, `TABLE 11`,
  published metadata, official/Google PDF identities, and all eight drawing sheets per document.
- Both publications independently yield five exact prescriptions. Each has 12 ordered surfaces,
  six aspheres, nine A4-A20 coefficients per asphere, and a complete optical-cell digest. The
  source-defined full FOV is divided by two for the stored half field; no missing number is
  inferred and the absent source wavelength is not represented as published provenance.
- Attempts 3/4 convert all ten publication/embodiment items to staging-only ZMX. Their result
  semantic hashes are `80fe2ec7b62510b6718d5d9b3737e281f3a166a77d7b50c236b4c5de26d7e820`
  for `US-12216247` and `6a6b6c9805e617d412eaebadd553b81a9b2f2230dfa0637290e8d3ca31fe9b3d`
  for `US-20220244497`; each matches across both attempts after the explicitly listed retry fields
  are removed and the receipt is replaced by its semantic hash. All 20 receipts, requests,
  responses, and candidate/staging ZMX pairs were rehashed from disk with zero mismatch.
- The worker traces 5/5 finite edge-field rays through all 12 surfaces for every item, but traced
  image heights differ from the published ImgH values. The discrepancy remains evidence for the
  pending real-IMH/quality/deduplication/route intake gates; nothing is called accepted or good.
- Result set: `e4264482831b0e1bf06dd619cb7ded9ee649ee57c4f4c99eef7f300c739e1150`.
  Strict audit is 619/619, missing=0, corrupt=0. Summary/report SHA-256 values are respectively
  `5f4559562e3c1989571eea13b4f239081ce139897fb755a09b3d0fc8a50ea561` and
  `81c85133ded721445f5555a92d5c189d2124b4fdaace8dcd98dae8d79953207b`.
- Sunny census falls from 187 items / 51 roots to 177 / 49. Two fresh builds and the retained
  after artifact are byte-identical at
  `a7f8bea5e3b9f79beb1cd9a684d5b5fb9b399ebb7486455e83ffaf2f87231b74`.
- All 307 offline patent parser/replay/census/saturation/process/pool tests, Ruff, and
  `git diff --check` passed with the non-`real_machine` subprocess guard active. The frozen
  snapshot/audit/cohort hashes remain
  `c86527b71e0500074bf14e1668bc3ab6701e5d54d3d22ef5826686101d6b5ec1`,
  `110ad2f15019982154851950c5a4ea0f23b4a734ffe8aeb144a5ffa45d290143`, and
  `e809823c709de93f49eb9b2103c4ebcdd9cf7e34d88f45a4953aaa21fd7bb42b`.
- `generic_summary_metadata_missing` at 183 is now the largest executable parser bucket, ahead of
  Sunny 177 and AAC Raytech 174. CODE V inventory stayed zero. Full host pytest is not claimed;
  final complete integration truth remains GitHub CI.

Parent patent saturation and formal intake remain incomplete.
