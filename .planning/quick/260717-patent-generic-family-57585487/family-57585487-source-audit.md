# Family 57585487 Source Audit

## Identity

The frozen root `US-20180094086` resolves to exact publication `US-20180094086-A1`,
application `15/821363`, Family ID `57585487`, filed 2017-11-22 and published
2018-04-05. The official PPUBS source names FUJIFILM Corporation as applicant and assignee,
binds continuation parent `PCT/JP2016/062246`, and binds Japanese priority applications
`JP2015-126879` and `JP2016-058470`.

## Complete denominator

The exact official source contains paragraphs `[0001]` through `[0620]`, 24 individually
headed `Synthesis Example` sections, cut-filter manufacturing Examples 1 through 32, one
Comparative Example, claims 1 through 18, FIGS. 1 through 4 on two drawing sheets, and one
source table. The 105 generic `Example` anchors are repeated prose/table references; they are
not 105 optical embodiments.

FIG. 1 is a camera-module block drawing with a generic imaging lens numbered 14. FIGS. 2-4
are near-infrared-cut-filter layer stacks. TABLE 1 has 33 rows for Examples 1-32 and Comparative
Example 1, with only heat-resistance ratings at 400/800 nm and solvent-resistance ratings.

## Prescription exclusion

The source-locked normalized text contains zero radius-of-curvature/curvature-radius markers,
numbered optical surfaces, refractive-index fields, Abbe fields, asphere data/coefficient fields,
focal-length fields, F-number/FNO fields, or field-of-view/HFOV fields. Its 11 generic `lens`
tokens describe possible filter end uses or FIG. 1's unprescribed imaging lens; none publishes an
ordered optical surface prescription.

All 114 pages of two independently fetched USPTO wrappers and the Google wrapper were decoded.
Every wrapper has one page image on every page, and all 114 canonical page rasters agree. The
all-page contact review confirms two drawing sheets, chemical structures/reaction schemes through
the specification, TABLE 1 on PDF pages 112-113, and claims beginning on page 113. No hidden
optical table exists.

## Disposition

The single document item is terminal as
`confirmed_no_prescription.near_ir_absorbing_polymer_and_cut_filter_materials_only`. It creates
no conversion worker request, receipt, fingerprint, candidate, or ZMX. WO, JP, and TW family
publications directly evidenced outside the frozen 619-root cohort are durably queued; their data
is not borrowed into this US source classification.
