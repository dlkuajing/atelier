# Quick Task: Patent Generic Family 57585487

**Status:** complete-shovel-saturation-incomplete
**Parent:** patent saturation engineering (active / incomplete)
**Started:** 2026-07-17
**Entry evidence:** strict 619/619 replay ledger with
`generic_summary_metadata_missing` at 149 items / 149 roots and no corrupt result.

## Objective

Resolve deterministic next Family ID `57585487`, root `US-20180094086`, publication
`US-20180094086-A1`. Reconcile exact publication/application/family identity, every disclosed
example or embodiment, all figures/tables, required system metadata, and the complete official
PDF denominator before choosing a deterministic source-proven terminal outcome or continued
parser review.

## Plan

1. Freeze the 149-item generic census, official HTML identity, family/application/priority facts,
   and attempt-1 document-level failure.
2. Establish the exact example/embodiment/table/figure denominator and distinguish optical
   prescriptions from material, filter, measurement, module, or device disclosures.
3. Retrieve the official PDF independently twice, inspect every decoded page and drawing/table
   sheet, and durably queue directly evidenced publications outside the frozen cohort.
4. Add one exact-source parser or terminal profile only if every disclosed item and required
   field is accounted for. Preserve source values and fail closed on malformed, conflicting, or
   absent cells; never infer or borrow numeric data.
5. Replay the root append-only twice, compare canonical semantics excluding only permitted retry
   identity, audit all roots, and rebuild the after generic census twice.
6. Run offline patent/guard tests, Ruff, Python compilation, and `git diff --check` with
   `PYTHONUTF8=1`, active non-`real_machine` guard, and CODE V inventory zero.
7. Update STATE, decisions, source/raster evidence, external-family queue, and recovery cursor.
   Commit this shovel, then remeasure every executable bucket. Parent saturation remains
   incomplete.

## Entry facts

- Queue order identifies `US-20180094086-A1`, Family ID `57585487`, with layout signature
  `0a4cc8e64f59fbadc5064e95adb03a86149542d55bad57c5a03acfe6eb056c84`.
- Attempt 1 is one document-level `generic_summary_metadata_missing` parser review; the frozen
  census reports 105 example anchors, one PPUBS table, and no effective-focal-length, F-number,
  or angular-field marker.
- The title and abstract describe a near-infrared absorbing composition, cut filter,
  copper-containing polymer, and related devices/manufacturing methods. This is entry evidence,
  not yet a terminal classification.
- Entry strict audit is 619/619 with corrupt=0; worktree is clean, branch is 0 behind / 83 ahead
  of `origin/main`, and CODE V inventory is zero.

## Completion evidence

- Exact PPUBS HTML binds paragraphs 0001-0620, 24 polymer synthesis examples, 32 near-IR
  cut-filter manufacturing examples, one comparative example, FIGS.1-4 on two drawing sheets,
  one 33-row heat/solvent-resistance table, and claims 1-18.
- Two independently fetched official PDFs and the Google PDF each contain 114 image pages; all
  114 decoded rasters agree. The all-page contact review exposes chemical structures and filter
  layer stacks, with no hidden optical prescription table.
- The source-locked text has zero curvature/surface/index/Abbe/asphere/focal-length/F-number/field
  prescription markers. The root becomes one
  `confirmed_no_prescription.near_ir_absorbing_polymer_and_cut_filter_materials_only` terminal
  and creates no worker request, receipt, fingerprint, candidate, or ZMX.
- Attempts 2/3 are semantic-equal excluding only `result_attempt` at
  `3b7df309...074ad6`. Generic census is byte-identical across two rebuilds at 148 roots/items;
  result set is `f0745d7a...61a5b`, summary is `d69b7c61...bf7c4`, and strict audit is 619/619
  with corrupt=0.
- 425 offline patent/guard tests pass. WO, JP, and TW family publications are queued outside the
  frozen cohort. Generic remains the largest executable bucket, and Family `93653416`, root
  `US-12663910`, is next. Parent saturation remains active and incomplete.
