# Quick: Patent generic Family 63585563

**Status:** complete-shovel-saturation-incomplete
**Parent:** patent saturation engineering (active / incomplete)
**Started:** 2026-07-16
**Entry evidence:** strict 619/619 replay result set
`5469f10337a7417451c8b2f1ee023642ca91cda47bc163c6a61360ea9f972243`, with
`generic_summary_metadata_missing` at 171 items / 171 roots and no corrupt result.

## Objective

Resolve the deterministic next exact same-layout family cluster, Family ID `63585563`:
`US-11435552` and `US-20200041761`. Reconcile publication/application/family identities, every
declared example or embodiment, all 11 tables, every required optical system field, and drawing
evidence before choosing deterministic conversion, a source-proven terminal, or continued parser
review. The shared layout signature is queue evidence only.

## Plan

1. Freeze the 171-item generic census and source-lock both official documents, their relationship,
   source denominator, and current two-item failure set.
2. Audit every table, figure, example/embodiment anchor, surface/asphere row, optical layout, and
   system-metadata binding; acquire official raster evidence for image-only content if required.
3. Implement only exact-source fail-closed behavior. Never infer, repair, interpolate, or derive a
   missing optical value from a drawing, formula, range, or family peer.
4. Replay both roots append-only twice, compare canonical semantics excluding only permitted retry
   identity, audit all 619 roots, and rebuild the after generic census twice.
5. Run offline patent/guard tests, Ruff, and `git diff --check` with `PYTHONUTF8=1`, active
   non-`real_machine` guard, and CODE V inventory zero.
6. Update STATE, decisions, this plan, and the recovery cursor with exact hashes. Commit this
   shovel, then remeasure all executable buckets. Parent saturation remains incomplete.

## Completion evidence

- Exact official B2/A1 records share Family ID `63585563`, application `16/485921`, title, and
  Samsung ownership; B2 Prior Publication Data names A1. Raw/normalized HTML and all 11 ordered
  table blocks are pinned independently per publication.
- Three numerical embodiments each disclose visible and IR moving-group states, reconciling to six
  ledger items. All 18 declared figures, 18 drawing sheets, 11 tables, and three narrative/system
  bundles are accounted for.
- Official rasters confirm that TABLE 7 really has a duplicate `K`, TABLE 8 really prints radius
  `1.530f377`, and TABLES 6/10 really use inconsistent `1..10` asphere headers. No token, column,
  surface number, system value, or wavelength is repaired or inferred.
- The first visible/IR states parse to fingerprints `2f0f22ea16129b08` and `60e6e642c0aa3840`.
  The worker returns source-proven `trace_timeout` and `trace_failed` receipts respectively and
  publishes no ZMX. The four remaining states retain precise source-conflict parser reviews.
- Two live PDF wrapper downloads differ per publication, while all 34 decoded rasters are equal
  within each publication. Both wrappers, stable raster-set hashes, and contact sheets are retained
  and rehashed by tests.
- Attempts 2/3 are append-only and semantic-equal after excluding result/conversion attempt
  identity and replacing receipt path/hash with receipt semantics. Root semantic SHA-256 values are
  `6966236f5c289316c1f20abbf75557cfac9d35547de44b4c52017bb12256407f` and
  `6f0531c581ea896dc11df7819efdf93c5b9c14d33d8d1d51bf70c97f708730bb`.
- Result set is `8ed2cd5a9ae470f96f4fa2dba96581a4430cd2a1c4f4affa807ef19d84275dd8`.
  Strict audit is 619/619, missing=0, corrupt=0. Summary/report SHA-256 values are
  `8e024e9428b824f0ddb91f2176f6ceb57c0c6bfc7f815436d71e85dd3cb0e75b` and
  `f0f3504e971910bf6bd9af9fb0f16f715187d600da4ab92cdd2c0456215fce8f`.
- Generic census falls from 171 items/roots to 169/169. Two fresh after builds are byte-identical
  at `d0954acc3740191b51828a8714082029c7d8bfc7e21073d7b30806afaac26b91`.
- All 353 offline patent parser/replay/census/saturation/process/pool/crawl-QC/guard tests, Python
  compilation, focused Ruff, and the CODE V guard pass with CODE V inventory zero.
- Deterministic queue recomputation keeps generic first at 169 nonterminal roots. The next
  two-root exact family by stable layout/family ordering is Family ID `85199256`:
  `US-12517281` and `US-20260093056`, layout
  `437fa2555cbad39206a20353ea2dc5e5c7dba01ccf65749ed1c49baa7cfdaa4b`.

Parent patent saturation, the external-family queue, and formal intake remain incomplete. Full
host pytest is not claimed; complete integration truth remains GitHub CI.
