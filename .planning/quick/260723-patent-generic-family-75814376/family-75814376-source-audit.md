# Family 75814376 source audit

## Exact source

- Retained publication: `US-20260169267-A1`, application `19/351312`, Family
  `75814376`, filed 2025-10-07 and published 2026-06-18.
- Retained official HTML: `96755` bytes, SHA-256
  `acd675d2fa1773a55594c3132b04c3572a09ea6202cc90434e7fb9fdbf1f6119`.
- The source identifies Chinese priority `202110159404.5` (2021-02-05), parent
  continuation application `17/211830` (2021-03-25) and parent grant document
  `US-12461343`. Those lineage records are bibliography only.
- Family `94742160` / `US-20260177783-A1` shares the queue layout signature but is
  a different family. It is an explicit exclusion, not a source of item boundaries,
  values or outcomes.

## Complete denominator

- Cross-reference paragraph: `[0001]`; Background: `[0002]-[0005]` (4);
  Summary: `[0006]-[0016]` (11); Description: `[0017]-[0158]` (142).
- The exact raw markers are continuous and each occurs once. The retained source has
  two unescaped `EFL<Fno` tokens before `[0012]` and `[0140]`; whole-section HTML
  normalization hides those two following markers. The source is not repaired.
  Reconciliation and classification split the exact raw markers first and normalize
  isolated paragraphs afterward.
- Claims: 20; independent claims `1` and `14`; dependent graph frozen in the
  computed-facts artifact. Figure-reference tags: 151; declared panels: 69;
  drawing sheets: 30; MathML objects: 22.
- Native HTML tables, `TABLE-US` blocks, embedded custom-character images and
  inline-formula lead/tail pairs: zero.
- Baseline/common description occupies `[0017]-[0103]`. Nine disjoint numerical
  source items occupy `[0104]-[0134]`: item 1 is `[0104]-[0118]`; items 2-9 are
  consecutive two-paragraph spans `[0119]-[0134]`. Common parameter material is
  `[0135]-[0158]`.
- Baseline FIGS. 1-5 are not prescriptions. Item 1 owns FIGS. 6, 7A-7D, 24 and 25;
  items 2-9 follow the same exact two-figure cadence through FIGS. 22, 23A-23D,
  40 and 41. Common FIG. 42 is not an additional prescription.

## Optical evidence boundary

- The exact HTML directly publishes item-specific EFL, HFOV, TTL, Fno and ImgH for
  all nine items.
- Complete ordered surface radii, axial spacings, numerical material/index/Abbe
  rows, aperture-stop rows, conics and asphere coefficients are published only in
  the official PDF's one-bit raster FIGS. 24-41. Common FIG. 42 is also raster.
- Two independent official PDF downloads have different wrapper SHA-256 values
  (`c30677bc...188f3` and `4b9a714d...a1edb`) but the same 43-page decoded-raster-set
  SHA-256 `8ab3fbb9...22378`. Every page contains exactly one one-bit raster and the
  PDF text-layer character count is zero.
- Official originals were inspected only for layout and printed-token categories.
  The contact sheet is navigation-only. No OCR, enhancement, repair, measurement,
  raster numeric transcription or raster numeric inference was used.

## Classification

All nine numerical prescriptions are published, but none is machine-readable under
project evidence policy. Each item therefore closes as
`metadata_unpublished.machine_readable_prescription_and_system_tables_absent`.
This describes the access-policy boundary; it does not deny the printed prescription.
No worker, CODE V call, conversion request, receipt, formal ZMX or prescription
fingerprint is authorized. Any source, raw-marker, anomaly, claim, figure, MathML,
PDF wrapper, page-role or decoded-raster drift fails the exact family closed.
