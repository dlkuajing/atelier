# Family 100312404 source audit

## Exact source and lineage

- Retained publication: `US-20260186252-A1`, application `19/292973`, Family
  `100312404`, filed 2025-08-07 and published 2026-07-02.
- Retained official HTML: `117302` bytes, SHA-256
  `cb2188a9d66b715c5530c72647e069e7e5bc5f17bd5ffa94164bbbb4cc05acd2`.
- The exact Related U.S. Application Data identifies continuation parent
  application `PCT/CN2024/144649`, filed 2024-12-31, but publishes neither a
  parent publication identifier nor a separate priority record. The application
  identifier is bibliography only and supplies no optical value.

## Complete denominator

- Technical Field: `[0001]` (1); Background: `[0002]-[0003]` (2); Summary:
  `[0004]-[0039]` (36); Brief Description of Drawings: `[0040]-[0068]` (29);
  Detailed Description: `[0069]-[0201]` (133). All 201 markers are continuous
  and unique.
- Claims: 10; claim `1` is the only independent claim. Figure-reference tags:
  90; declared panels: FIGS. 1-28; drawing sheets: 15; MathML objects: 52.
- Native HTML tables and custom image tags: zero. The specification contains
  exactly 15 flattened `TABLE-US` blocks, numbered 1-15 without gaps.
- Common definitions occupy `[0069]-[0108]`. Seven disjoint numerical source
  items follow: first embodiment `[0109]-[0164]`, second `[0165]-[0170]`, third
  `[0171]-[0176]`, fourth `[0177]-[0182]`, fifth `[0183]-[0188]`, sixth
  `[0189]-[0194]`, and comparative embodiment `[0195]-[0200]`. Paragraph
  `[0201]` is common closing material.
- Each item owns four consecutive panels, one odd-numbered R1-R14
  surface/material/spacing table and the following even-numbered R1-R12
  conic/A4-A30 table. TABLE 15 is common and supplies the seven direct system
  columns; it is not an eighth prescription.

## Optical evidence boundary

- Every odd surface table has the exact ordered labels
  `R1,R2,R3,R4,ST,R5,...,R14`. Its ST row prints infinity and slash/blank cells,
  with no axial distance or thickness. Paragraph `[0100]` says only that ST is
  arranged between L2 and L3; paragraph `[0132]` defines `d4` as the full
  R4-to-R5 distance. Therefore no item directly publishes where ST lies inside
  that interval. ST is not made coincident with R5 and `d4` is not split.
- The fourteen published `d1`-`d14` values sum exactly to each published TABLE
  15 TTL. This consistency check does not locate ST and is not used to derive a
  missing coordinate.
- TABLE 4's third coefficient segment directly contains malformed normalized
  token `1.12590-05` at R12/A24. The token is preserved and not repaired from
  the official raster.
- Fifth-embodiment paragraph `[0188]` directly says `IH=3.268 mm`; TABLE 15
  directly says `IH=3.135`. Both values are preserved and neither is selected.
- Two official PDF downloads are distinct wrappers with SHA-256
  `a0f39d5e...508da` and `5bedb978...d3091`, but decode to the same 32-page
  one-bit raster set at `81de45c8...3048e5`. Physical pages 2-16 are the 15
  drawing sheets; pages 17-31 contain specification pages 1-15; claims begin on
  page 31 and end on page 32.
- Official originals were inspected only for cover, drawing, specification,
  table and claim layout plus the ST row's blank-cell topology. The contact
  sheet is navigation-only. No OCR, enhancement, repair, measurement, raster
  numeric transcription or raster numeric inference was used.

## Classification

Items 1, 3, 4, 6 and 7 close as
`metadata_unpublished.prescription_specific_stop_surface_position_absent`.
Item 2 additionally records the malformed A24 token; item 5 additionally records
the direct image-height conflict. No worker, CODE V call, conversion request,
receipt, prescription fingerprint, formal optical case or ZMX is authorized.
Any exact-source identity, denominator, item/table binding, printed-token anomaly,
PDF wrapper, page-role or decoded-raster drift must fail closed.
