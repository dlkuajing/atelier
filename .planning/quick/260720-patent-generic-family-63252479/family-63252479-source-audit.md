# Family 63252479 source audit

The retained content authority is the exact USPTO Patent Public Search A1 HTML for
`US-20250284086-A1`, application `19/218688`, Family ID `63252479`. Its raw and
normalized hashes, five section hashes, all 391 continuous numbered paragraphs,
52 declared figure panels, 20 claims, eight flattened table objects and two MathML
objects are source-locked by the classifier and derived profile.

The independent source-item denominator is 30: four exact numeric prescriptions,
21 mechanical lens-module embodiments or variants, and five camera/process/device/
claim wrappers. Tables 1-8 publish 51 ordered surface rows and 36 coefficient rows.
The four prescriptions directly publish EFL, TTL, f-number, sensor diagonal and BFL,
but none publishes a prescription-specific numeric angular field. Items 1 and 3 have
publication-local stop bindings; items 2 and 4 do not. No angular field is derived
from sensor diagonal/EFL and no value is borrowed from a related application.

Table 7's printed object contains rows 1-13 despite paragraph 154's generic reference
to line 0. Table 8 contains printed blank coefficient cells. Neither condition is
repaired or imputed. Two official PDF fetches have different wrapper hashes but the
same byte count and identical 47 decoded original-raster page records. Original
rasters were used only to corroborate layout and printed-token boundaries: no image
enhancement, OCR repair, drawing measurement or raster numeric transcription was used.

Replay attempts 2 and 3 are semantically identical after removing only
`result_attempt`. The four prescriptions close as precise `metadata_unpublished`
terminals; the remaining 26 items close as precise `confirmed_no_prescription`
terminals. There are no conversion requests, receipts, candidate/staging ZMX files,
formal intake items or CODE V calls. Global patent saturation remains incomplete.
