# Quick Task: Patent Generic Family 61244801

**Status:** complete-shovel-saturation-incomplete
**Parent:** patent saturation engineering (active / incomplete)
**Started:** 2026-07-17
**Entry evidence:** strict 619/619 replay ledger with
`generic_summary_metadata_missing` at 133 items / 133 roots and no corrupt result.

## Objective

Resolve deterministic next Family ID `61244801`, root `US-11287601`, publication
`US-11287601-B2`. Reconcile exact publication/application/family identity, every disclosed
imaging-lens-assembly embodiment, all figures/tables/formulas, optical prescription markers,
related applications and the complete official PDF denominator before allowing conversion or a
source-proven terminal/nonterminal outcome.

## Plan

1. Freeze the 133-item generic census, official HTML identity, application/priority/related-family
   records, and attempt-1 document-level failure.
2. Establish exact section, paragraph, embodiment, claim, figure, table, equation, material,
   numeric-data and prescription-marker denominators. Bind every disclosed optical condition to
   its source context.
3. Retrieve the exact official B2 PDF independently twice and attempt an independent public
   wrapper. Inspect every decoded page and drawing/table sheet without deriving values from
   drawings or borrowing numeric truth across publications.
4. Add one exact-source parser or terminal/nonterminal profile only if every disclosed item and
   required prescription field is accounted for. Fail closed on absent, malformed, conflicting,
   image-only or derivation-only data.
5. Replay the root append-only twice, compare canonical semantics after only permitted result
   identity normalization, audit all 619 roots, and rebuild the after generic census twice.
6. Run offline patent/guard tests, Ruff, Python compilation and `git diff --check` with
   `PYTHONUTF8=1`, active non-`real_machine` guard and CODE V inventory zero.
7. Update STATE, decisions, source/raster evidence, external-family queue and recovery cursor.
   Commit this shovel, then remeasure every executable bucket. Parent saturation remains
   incomplete.

## Entry facts

- Queue order identifies `US-11287601-B2`, Family ID `61244801`, layout signature
  `1d4d179f22873560d591583b851d758fb28044fbbb4fee8b2721e31e97faf1d8`.
- The frozen raw-document SHA-256 is
  `7910d5bca19dc438a5ca8b159eb45327adc1e3aff91670babfde68745c4e8fd3`.
- Attempt 1 is document-level `generic_summary_metadata_missing` with
  `PatentParseError: embodiment f/Fno/HFOV line not found`.
- Retained official source path is
  `data/patent-lake/uspto-ppubs-html/USPAT/7910d5bca19dc438/US-11287601-B2.html`.
- Entry worktree is clean at commit `74411a8921bdadcf686d1df00db4868c53544ca2`, branch is
  0 behind / 98 ahead of `origin/main`, and CODE V inventory is zero.

## Completion evidence

- Exact official B2 source binds application `16/483973`, Family ID `61244801`, PCT
  `PCT/US2018/017252`, paragraphs 1-87, claims 1-18, FIGS.1-8, eight drawing sheets, five raster
  data tables, two source-named Sample Designs, 37 claim-style Examples and one mobile-device
  wrapper. `family-61244801-example-denominator.json` reconciles all 37 Examples to three ledger
  items with zero unmapped examples and zero synthesized coordinates.
- Two independently fetched official B2 wrappers and the Google wrapper contain 19 pages and
  agree on every decoded 3300×2560 raster. The official wrappers have no text layer; Google's text
  layer is an auxiliary exact-raster OCR view only. All pages were decoded, and prescription pages
  5-9 were inspected at full resolution.
- Sample Design 1's expected 60 exponent-bearing coefficient cells expose only 51/51 markers in
  Google/RapidOCR plus 15 joined tokens and no design-specific EFL/F-number/field/image height.
  Sample Design 2 directly publishes EFL 1.57 mm, F/2.4, 115-degree diagonal field and 1.98 mm
  image height, but its 50 expected cells expose only 48/33 markers plus 10 joined tokens. Both
  remain exact parser reviews without manual coordinate repair. Example 37/FIG.8 becomes one
  `confirmed_no_prescription.electronic_device_wrapper_only` terminal.
- Attempts 4/5 are append-only and semantic-equal after excluding only `result_attempt`, at
  SHA-256 `38c9258455e5aba43e85f9a1a235ea1c9d148e47a8234c198ac43a5785331b35`.
  The root is `mixed_nonterminal` with two parser reviews and one terminal; no worker, request,
  receipt, fingerprint, candidate or ZMX is created.
- Result set is `34dc2a55b427fa8b75525be4a86a312a65599663a0ac3083cf81415b0fb7a864`;
  summary/report hashes are `d6c329f3399841357eed918a2b52771f8003cbeb7ee6260d203fd53ddc14a8a4`
  and `21530397ed8fe691b0628ecf0d155214ce1fea675af5c666b5d55833d8767c38`.
  Strict audit is 619/619, missing=0, corrupt=0.
- Generic census falls 133→132 roots/items. Two fresh after builds are byte-identical at
  `cc6bd6ecfb513873ae7b079b45187b44607ef9732b510e3eed62c72844f449d1`.
- Focused source/parser/raster/replay/census tests pass 3/3. All `test_patent*.py` plus the offline
  CODE V guard pass in two explicit selections, 495/495 total; guard self-test passes 5/5. Python
  compile, Ruff, JSON validation and final `git diff --check` pass with CODE V inventory zero.
- Same-application, PCT, CN, EP, KR and continuation publications are queued outside the frozen
  cohort. The next deterministic generic family is `68533575`, root `US-20230418018`, publication
  `US-20230418018-A1`.

Parent patent/source saturation, coordinate recovery, conversion, intake, routing, independent
review, PR and CI remain incomplete.
