# Family 81110170 source audit

## Exact source

- Retained publication: `US-20260169260-A1`, application `19/370790`, Family
  `81110170`, filed 2025-10-28 and published 2026-06-18.
- Retained official HTML: `115004` bytes, SHA-256
  `e338a627054e622bdbc59c7ef899fc7bcdb1c62bb42cd88bcc2c71f0f68f2d4a`.
- The source identifies one U.S. continuation parent, application `17/861270`
  (filed 2022-07-11; parent grant `US-12481126`), and Chinese priority
  `202210032698.X` (2022-01-12). These records are bibliography only.
- Five other publications share the queue layout signature but belong to five
  different families. They are explicit exclusions, not sources of item boundaries,
  values or outcomes.

## Complete denominator

- Background/summary paragraphs: `[0001]-[0014]` (14).
- Description paragraphs: `[0015]-[0168]` (154).
- Claims: 20; independent claims `1`, `8`, `15`; dependent graph frozen in the
  computed-facts artifact.
- Figure-reference tags: 153; declared panels: 70; drawing sheets: 31.
- MathML objects: 22. Native HTML tables, `TABLE-US` blocks, embedded custom
  character images and inline-formula lead/tail pairs: zero.
- Shared definition/drawing material occupies `[0015]-[0103]`. Nine disjoint source
  items occupy every paragraph from `[0104]` through `[0138]`: item 1 is
  `[0104]-[0122]`; items 2-9 are the consecutive two-paragraph spans
  `[0123]-[0138]`. Common parameter/relation material is `[0139]-[0168]`.
- Each item owns one cross-section panel, four aberration panels, one optical-data
  figure and one aspheric-data figure. Baseline FIGS. 1-5 and common FIGS. 42-43
  are not additional prescriptions.

## Optical evidence boundary

- The exact HTML directly publishes item-specific EFL, HFOV, TTL, Fno and ImgH for
  all nine items.
- The ordered surface radii, axial spacings, numeric material/index/Abbe rows,
  aperture-stop row, conics and asphere coefficients are published only in the
  official PDF's one-bit raster figures 24-41. Common figures 42-43 remain raster
  system/ratio tables.
- Two independent official PDF downloads have different wrapper SHA-256 values
  (`78e7650e...9b50` and `074df416...8c4a`) but the same 45-page decoded-raster-set
  SHA-256 `ea76d76e...0c87`. Every page contains exactly one one-bit raster and the
  PDF text-layer character count is zero.
- Official originals were inspected only for layout and printed-token boundaries.
  The contact sheet is navigation-only. No OCR, enhancement, repair, measurement,
  raster numeric transcription or raster numeric inference was used.

## Classification

All nine numerical prescriptions are published, but none is machine-readable under
the project evidence policy. Each item therefore closes as
`metadata_unpublished.machine_readable_prescription_and_system_tables_absent`.
No worker, CODE V call, conversion request, receipt, formal ZMX or prescription
fingerprint is authorized. Any source, paragraph, claim, figure, MathML, PDF wrapper,
page-role or decoded-raster drift fails the exact family closed.
