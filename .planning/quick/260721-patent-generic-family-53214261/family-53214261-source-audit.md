# Family 53214261 exact-source audit

## Identity and denominator

- Frozen root / publication: `US-9618727` / `US-9618727-B2`.
- Application / family: `14/832479` / `53214261`.
- Title: `Imaging lens assembly`.
- First inventor: `Jang; Issac`; co-inventor: `Park; Jay`; assignee:
  `AAC TECHNOLOGIES PTE. LTD.`.
- Same-application publication: `US-20160161708-A1`; Korean priority:
  `10-2014-0174423`, 2014-12-05.
- Exact retained official HTML: 31,181 bytes,
  SHA-256 `db0e5d448f3107df967d4c6b637fd035971b62dadfb78f27de889b9d5a0ab6a6`.
  Its normalized 17,989-character text is
  `aa6d8cdbc0784e7c42022f8cf1d6d3526e25a3e9e0f928abb610b76fccf2d4d6`.
- Source denominator: four Background/Summary paragraphs, six drawing-description
  paragraphs, 36 detailed-description paragraphs numbered 7-42, claims 1-7,
  five tagged tables, nine MathML objects, 19 figure references and five figures on
  four drawing sheets.
- The complete optical-item denominator is two and only two items: Embodiment 1 and
  Embodiment 2. Claims describe their common four-lens assembly and do not add a
  separately parameterized optical prescription.

## Published optical content

- TABLE 1 and TABLE 3 each publish object, stop, surfaces 2-11 and image plane.
  Surfaces 2-6, 8 and 9 are aspheres; surface 7 and the two filter faces 10/11 are
  spheres. The ordered rows include radii, spacings and the published material rows.
- TABLE 1 directly prints `f = 2.0813 mm`, `Fno = 2.2`, `DOF = 42.7872°` for
  Embodiment 1. TABLE 3 directly prints `f = 2.085 mm`, `Fno = 2.2`,
  `DOF = 42.7895°` for Embodiment 2.
- Paragraph 20 explicitly defines `DOF` as depth of field. The source contains no
  `field of view`, `FOV` or `half field` phrase, so the printed DOF values are not
  relabeled as angular field.
- TABLE 2 and TABLE 4 each visually and textually contain seven anonymous physical
  coefficient rows under `K, a4, a6, a8, a10, a12, a14, a16`, but no surface-number
  column or row label. The first two rows have blank `a16` cells; each table therefore
  publishes 54 populated coefficient values, not a complete labeled 7-by-8 mapping.
- The text defines `y` as maximum image height but never directly publishes its
  absolute value. TABLE 5 publishes only the rounded ratio `TTL/2y = 0.82` for each
  embodiment. No absolute image height is derived from that ratio, the thickness
  stack, focal length or any drawing.

## Terminal classification

Both source items close as
`metadata_unpublished.unlabeled_asphere_surface_binding_and_absolute_image_height_and_angular_field_absent`.
Each item has substantial optical data, so `confirmed_no_prescription` would be wrong;
however, assigning anonymous coefficient rows to particular aspheric surfaces,
deriving absolute image height, or interpreting DOF as angular field would synthesize
unpublished metadata. The classifier is bound to the exact raw and normalized source
hashes, section and raw-section hashes, paragraph/claim/table/MathML/figure
denominators, formal table hashes, 54-cell coefficient counts and zero surface labels
inside both coefficient tables.

The result is source-terminal without worker, conversion request, receipt,
prescription fingerprint, candidate/staging ZMX, formal intake, CODE V use or expert
verdict.

## Official raster cross-check

Two independently downloaded B2 PDF containers differ at the wrapper level but decode
to the same nine-page canonical raster set. The same-application A1 is also nine pages
and has zero equal same-position pages with the B2 raster set. Original 2560x3300
rasters confirm the four drawing sheets, the anonymous seven-row coefficient layout,
the two blank `a16` cells per coefficient table, TABLE 5 ratios and the claims ending.

Review used original rasters only. It did not enhance images, repair OCR, measure
drawing geometry, transcribe numeric values from drawings, infer missing optical
metadata or borrow numeric content from the A1 or external family members. The exact
retained HTML remains the classifier's content authority.
