# Family 94589695 source audit

## Exact source

- Retained publication: `US-20260169264-A1`, application `19/034561`, Family
  `94589695`, filed 2025-01-23 and published 2026-06-18.
- Retained official HTML: `112450` bytes, SHA-256
  `fcf8c1d717e8d81f4617ac2e8e667c1e904cbe9d16de243598b77f90e0be22c2`.
- The source identifies Chinese priority `202411871173.0` (2024-12-18) and no
  U.S. related-application record. The priority record is bibliography only.
- Two other publications share the queue layout signature but belong to two
  different families. They are explicit exclusions, not sources of item boundaries,
  values or outcomes.

## Complete denominator

- Background/summary paragraphs: `[0001]-[0011]` (11).
- Description paragraphs: `[0012]-[0206]` (195).
- Claims: 20; independent claims `1`, `8`, `15`; dependent graph frozen in the
  computed-facts artifact.
- Figure-reference tags: 202; declared panels: 92; drawing sheets: 41.
- MathML objects: 31. Native HTML tables, `TABLE-US` blocks, embedded custom
  character images and inline-formula lead/tail pairs: zero.
- Shared drawing/definition material occupies `[0012]-[0123]`. Twelve disjoint
  source items occupy every paragraph from `[0124]` through `[0159]`: item 1 is
  `[0124]-[0137]`; items 2-12 are the consecutive two-paragraph spans
  `[0138]-[0159]`. Common parameter/relation material is `[0160]-[0206]`.
- Item 1 owns FIGS. 6A, 6B, 7A-7D, 30 and 31. Items 2-12 each own one even-numbered
  cross section in FIGS. 8-28, four following A-D aberration panels, one optical-data
  figure in FIGS. 32-52 and one aspheric-data figure in FIGS. 33-53. Baseline
  FIGS. 1-5 and common FIGS. 54-55 are not additional prescriptions.

## Optical evidence boundary

- The exact HTML directly publishes item-specific EFL, HFOV, TTL, Fno and ImgH for
  all twelve items.
- Complete ordered surface radii, axial spacings, numerical material/index/Abbe
  rows, aperture-stop and optical-bending-element rows, conics and asphere
  coefficients are published only in the official PDF's one-bit raster FIGS. 30-53.
  Common FIGS. 54-55 remain raster parameter/ratio tables.
- Two independent official PDF downloads have different wrapper SHA-256 values
  (`f6c895fe...17e87` and `c5f9b9fa...aae64`) but the same 55-page decoded-raster-set
  SHA-256 `60e0ecd3...9e273`. Every page contains exactly one one-bit raster and the
  PDF text-layer character count is zero.
- Official originals were inspected only for layout and printed-token categories.
  The contact sheet is navigation-only. No OCR, enhancement, repair, measurement,
  raster numeric transcription or raster numeric inference was used.

## Classification

All twelve numerical prescriptions are published, but none is machine-readable under
the project evidence policy. Each item therefore closes as
`metadata_unpublished.machine_readable_prescription_and_system_tables_absent`.
No worker, CODE V call, conversion request, receipt, formal ZMX or prescription
fingerprint is authorized. Any source, paragraph, claim, figure, MathML, PDF wrapper,
page-role or decoded-raster drift fails the exact family closed.
