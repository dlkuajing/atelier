# Family 69060231 exact-source audit

## Identity and lineage

The retained classification source is the official USPTO A1 HTML for
`US-20260089289-A1` (application `19/207416`, Family ID `69060231`). It names
Gal Shabtay, Eran Briman, Roy Fridman, Noy Cohen, Ephraim Goldenberg and Gil
Bachar as inventors and Corephotonics Ltd. as applicant. The retained related
data and paragraph `[0001]` directly print continuation parent application
`16/978,690` and grant `US-12,328,523`, international application
`PCT/IB2019/055734` filed 2019-07-04, and provisional application `62/693,951`
filed 2018-07-04. No optical number is borrowed from any of them.

## Complete source denominator

The exact A1 contains 33 Background/Summary paragraphs (`[0001]-[0033]`),
19 brief-drawing paragraphs (`[0034]-[0052]`), 24 detailed-description
paragraphs (`[0053]-[0076]`), twenty claims, zero tagged tables, zero MathML
objects, 55 `figref` tags and eighteen declared figure panels on nine drawing
sheets. Claims 1 and 20 are the only independent claims; both abstract the same
scanning/fusion system functions and add no separate lens prescription.

The detailed disclosure resolves to exactly six source items: vehicle
dual-camera system 100 (`[0053]-[0066]`), single-Tele system 400 and
Wide-plus-Tele system 400-prime (`[0067]-[0068]`), in-cabin systems 700 and
700-prime (`[0069]-[0072]`), and surveillance system 1000
(`[0073]-[0075]`). Paragraph `[0076]` is the closing paragraph. Shared
operation text and figures for the prime variants are retained only where the
source expressly applies them to both systems.

## Optical content and terminal classification

Paragraphs `[0054]-[0055]` identify generic Wide and Tele lenses and publish
only Wide FOV 50-100 degrees and Tele FOV 10-30 degrees. Paragraph `[0056]`
publishes the OPFE rest orientation and example actuator tilt limits. The A1
publishes no surface table, ordered surface sequence, radius, axial spacing,
numerical glass/index/dispersion, asphere coefficient, aperture-stop surface or
coordinate, numeric Wide/Tele focal length, image height or F-number.

All six items therefore close as exact `confirmed_no_prescription` architecture
terminals. The result does not derive a prescription from FOV values or drawing
geometry and does not import a lens from the parent application, PCT or cited
camera documents.

## Official PDF audit

Two independent official downloads are each 788,225 bytes and have distinct
container SHA-256 values because their creation metadata differs. Each has 15
pages, one 2560-by-3300 1-bit raster per page and no text layer. All fifteen
decoded page rasters are identical across both downloads and the pinned first
download; the ordered canonical raster-set SHA-256 is
`4a3e02c6d399a0f30daf9554c1722a549b597023a7aea0eb7493c71a071bd8ee`.

The retained PNGs are lossless RGB serializations whose three channels exactly
equal the decoded embedded page samples. The contact sheet was used only for
navigation. Original cover/drawing pages 1, 2, 4, 7, 9 and 10 were inspected;
they contain architecture blocks, flows and scene illustrations, not surface
data. No enhancement, OCR repair, drawing measurement, plot sampling, raster
inference or numeric transcription was used.

## Replay outcome

Append-only attempts 2 and 3 each retain six terminal items and have identical
business semantics after removing only `result_attempt`; semantic SHA-256 is
`b69882648ee8b8d5b136aaaf6821f40ea00118f626d5536091536e33d8b65bb3`.
No conversion worker, request, receipt, prescription fingerprint, candidate ZMX,
staging ZMX, formal intake or CODE V call was created. The strict replay audit is
619/619 with zero missing and zero corrupt results. The generic residual moves
from 44 to 43 roots/items; stable ordering selects Family `40641507`, root
`US-8134609`, next. Global saturation remains incomplete.
