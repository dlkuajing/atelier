# Quick Task: Patent Generic Family 95157884

**Status:** complete-shovel-saturation-incomplete
**Parent:** patent saturation engineering (active / incomplete)
**Started:** 2026-07-16
**Entry evidence:** strict 619/619 replay ledger with
`generic_summary_metadata_missing` at 150 items / 150 roots and no corrupt result.

## Objective

Resolve deterministic next Family ID `95157884`, root `US-20250216652`, publication
`US-20250216652-A1`. Reconcile exact publication/application/family identity, every disclosed
optical or mechanical state, all figures/tables, required system metadata, and the complete
official PDF denominator before choosing deterministic conversion, source-proven terminal
outcomes, or continued parser review.

## Plan

1. Freeze the 150-item generic census, official HTML identity, family/application/priority facts,
   and attempt-1 document-level failure.
2. Establish the exact embodiment/state denominator from headings, narrative, all 39 tables, and
   drawings; distinguish optical prescriptions from module/device architecture.
3. Retrieve the official PDF independently twice, inspect every page/drawing/table sheet, and
   durably queue directly evidenced publications outside the frozen cohort.
4. Add one exact-source parser or terminal profile only if every disclosed state and required
   field is accounted for. Preserve source values and fail closed on malformed, conflicting, or
   absent cells; never infer or borrow numeric data.
5. Replay the root append-only twice, compare canonical semantics excluding only permitted retry
   identity, audit all roots, and rebuild the after generic census twice.
6. Run offline patent/guard tests, Ruff, Python compilation, and `git diff --check` with
   `PYTHONUTF8=1`, active non-`real_machine` guard, and CODE V inventory zero.
7. Update STATE, decisions, source/raster evidence, external-family queue, and recovery cursor.
   Commit this shovel, then remeasure every executable bucket. Parent saturation remains
   incomplete.

## Entry facts

- Queue order identifies `US-20250216652-A1`, Family ID `95157884`, with layout signature
  `07d60ffb2fe30fe53684068648043ff5b7ebb66660dd4e09d8ff19c2ac67b61f`.
- Attempt 1 is one document-level `generic_summary_metadata_missing` parser review; the frozen
  census reports 241 embodiment anchors, 39 PPUBS tables, three F-number markers, 19 full-field
  markers, and no effective-focal-length marker.
- Entry strict audit is 619/619 with corrupt=0; worktree is clean, branch is 0 behind / 82 ahead
  of `origin/main`, and CODE V inventory is zero.

## Completion evidence

- Exact A1 HTML, all 39 source tables, 15 formal embodiments, 20 optical states, 64 figure
  panels, 62 drawing sheets, and two complete 106-page official PDF wrappers are hash-bound. The
  wrappers differ in bytes but all 106 decoded page rasters agree.
- Nine intact infinity states parse without numeric repair. Six produce staging-only ZMX files,
  while three terminate through trace-failure receipts. Nine finite-object macro states remain
  explicit nonterminal reviews because the current replay model is infinity-conjugate only.
- Official page 96 leaves the ninth embodiment's stop radius blank and conflicts on terminal
  surface numbering, so its two states terminate source-faithfully. Embodiments 11-15 terminate as
  imaging-apparatus/electronic-device wrappers with no additional prescription.
- Attempts 2/3 are semantic-equal after only permitted retry/receipt normalization at
  `1f471dff...b64c`. Generic census is byte-identical across two rebuilds at 149 roots/items;
  result set is `0955e458...b9429`, strict audit is 619/619 with corrupt=0, and the next exact group
  is Family `57585487`, root/publication `US-20180094086` / `US-20180094086-A1`.
- 421 offline patent/guard tests, Python compilation, Ruff, and `git diff --check` pass with CODE V
  inventory zero before and after. Parent saturation remains active and incomplete.
