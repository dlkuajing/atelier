# Family 61278233 exact-source audit

## Identity and denominator

- Frozen root / publication: `US-10642002` / `US-10642002-B2`.
- Application / family: `16/101650` / `61278233`.
- Title: `Camera optical lens`.
- Inventor: `Wang; Jianming`; applicant and assignee:
  `AAC Acoustic Technologies (Shenzhen) Co., Ltd.`.
- Same-application publication: `US-20190154989-A1`; Chinese priority:
  `CN 2017 1 1144123`, 2017-11-17.
- Exact retained official HTML: 42,832 bytes,
  SHA-256 `7217efb177dceaedc27238a4fbdd92619cfe1e6580959c2114322f98063dc26f`.
  Its normalized 30,580-character text is
  `2426cfd4e2ec7191d5bd8914894f44ffe517f8029f89cf9b32fc7821f9239791`.
- Source denominator: two Background paragraphs, nine drawing-description
  paragraphs, 107 detailed-description paragraphs numbered 10-116, claims 1-10,
  twelve flattened tables, zero MathML objects, 22 figure references and eight
  figures on four drawing sheets.
- The complete optical-item denominator is two and only two items: Embodiment 1 and
  Embodiment 2. Claims describe their common five-lens assembly and do not add a
  separately parameterized optical prescription.

## Published optical content

- TABLES 1/7 directly publish each embodiment's system focal length and five
  constituent-lens focal lengths.
- TABLES 2/8 each publish the aperture stop, R1-R12, d0-d12, five plastic-lens
  refractive-index/Abbe pairs, the filter pair and image spacing. The stop-to-R1
  distance is directly printed as `d0 = -0.326 mm` in both embodiments.
- TABLES 3/9 each publish ten surface-labeled rows R1-R10 with `k` and
  `A4,A6,A8,A10,A12,A14,A16`; there is no anonymous coefficient row or missing
  surface binding.
- TABLES 4/5 and 10/11 publish surface-labeled inflexion/arrest-point positions;
  TABLES 6/12 publish eight condition ratios. These auxiliary rows are not substituted
  for prescription or system metadata.
- Paragraphs 69/115 directly publish entrance-pupil diameter `1.83 mm`, full vision
  field image height `2.9335 mm` and diagonal field angles `81.43°` / `81.52°`.
- Paragraph 16 and claim 9 publish only the system-wide inequality
  `aperture F number ... less than or equal to 1.8`; the source contains no `FNO`,
  `F/#`, exact F-number assignment or numerical-aperture value. The two focal lengths
  and the common pupil diameter are not divided to synthesize embodiment F-numbers.

## Terminal classification

Both source items close as `metadata_unpublished.system_f_number_absent`. Each item
has a complete ordered surface/asphere prescription and direct field/image metadata,
so `confirmed_no_prescription` would be wrong. The upper bound `F-number <= 1.8` is
not an exact system datum, and the exact source profile therefore binds raw/normalized
hashes, four section hashes, contiguous paragraph and claim denominators, all twelve
table block/formal hashes, R1-R12 and R1-R10 row denominators, identity markers,
direct focal rows, direct field/image metadata and the two bound phrases.

The result is source-terminal without worker, conversion request, receipt,
prescription fingerprint, candidate/staging ZMX, formal intake, CODE V use or expert
verdict.

## Official raster cross-check

Two independently downloaded B2 PDF containers differ at the wrapper level but decode
to the same eleven-page canonical raster set. The same-application A1 is also eleven
pages and has zero equal same-position pages with the B2 raster set. Original
2560x3300 rasters confirm the four drawing sheets, both ordered surface tables, both
surface-labeled asphere tables, the direct image/field rows and claim 9's F-number
upper bound.

Review used original rasters only. It did not enhance images, repair OCR, measure
drawing geometry, transcribe numeric values from drawings, infer missing optical
metadata or borrow numeric content from the A1 or external family members. The exact
retained B2 HTML remains the classifier's content authority.
