# Quick: Patent generic Family 44121309

**Status:** complete-shovel-saturation-incomplete
**Parent:** patent saturation engineering (active / incomplete)
**Started:** 2026-07-16
**Entry evidence:** strict 619/619 replay result set
`e0b098b9b622c5ce6033889b93690aa18e5691a869309198a680c8f3ed74c180`, with
`generic_summary_metadata_missing` at 189 items/189 roots and no corrupt result.

## Objective

Resolve the tied-largest three-root official Family ID 44121309 without hiding the image-only
prescriptions explicitly declared in FIGS. 14A and 14B. Audit all three publication/application
relationships, every described optical design/table, official PDF layout, and deterministic OCR
evidence before permitting parser expansion or retaining source-specific review. Never infer a
terminal no-prescription result from the zero PPUBS HTML-table count.

## Plan

1. Freeze the 189-item generic census and bind the exact A1/B2 publications, applications, raw
   and normalized HTML hashes, and family relationships.
2. Audit every lens embodiment and drawing/table reference, especially the third projection and
   relay lens prescriptions in FIGS. 14A/14B and their system metadata requirements.
3. Acquire and retain official PDF evidence. Require exact page counts/layouts, decoded-raster
   identity where an independent overlay exists, explicit key-page labels, and unchanged
   0.95/0.99 OCR gates; use no LLM or inferred numeric repair.
4. Implement only exact-source fail-closed recovery/parser behavior. Preserve every incomplete,
   low-confidence, ambiguous, or unsupported finite-conjugate value as source-specific parser
   review, and generate no ZMX unless every required published cell is independently proven.
5. Replay all three roots append-only twice, compare canonical semantics excluding only retry
   identity, audit all 619 roots, and rebuild the after census twice.
6. Run parser/replay/census/saturation/process tests, Ruff, and `git diff --check` with
   `PYTHONUTF8=1` and CODE V inventory zero. Update STATE/decisions with exact hashes and the next
   measured bucket. Parent saturation remains incomplete.

## Completion evidence

- Full source/PDF/OCR audit: `family-44121309-source-audit.md`.
- All three exact official sources bind two spherical prescriptions in FIGS. 14A/14B, but publish
  no prescription-specific EFL or field. Generic F/6, about-F/3, and F/2.5-or-faster contexts are
  counted and rejected as substitutes.
- All official and Google decoded page rasters are pixel-identical. Exact page counts, blank
  overlay-page sets, key pages, 90-degree counterclockwise rotation, source hashes, table labels,
  and absence of OCR system labels are fail-closed.
- Each root expands to two terminals with reason
  `metadata_unpublished.prescription_specific_efl_and_field_absent`; no worker or ZMX exists.
- Attempts 2/3 are semantic-equal for all roots after excluding only `result_attempt`; see
  `family-44121309-replay-determinism.json`.
- Result set: `dbf8a68e115fc09d2ffe5ad90e4118548360869c528c43f1f1cec8988790469a`.
  Full audit: 619/619, missing=0, corrupt=0.
- Generic census: 189 to 186 items/roots. Both after builds are byte-identical at
  `f47b2a27a052dcb21ffab70975f1c5e7ae7e32aa80c16c47ed4aabfb1b32106d`.
- The frozen saturation snapshot was left byte-identical to its cohort pin. A trial rebuild was
  rejected because it incorporated replay staging and correctly invalidated the immutable cohort
  snapshot hash; its three generated baseline changes were removed before final audit.
- Verification: 207 complete patent-parser tests plus 61 replay/census/saturation/process/guard
  tests passed; Ruff and `git diff --check` passed; CODE V inventory was zero before every sweep.
- Next measured generic family is `46327306`, with roots `US-20080198482`, `US-7365917`, and
  `US-7859769`.

Parent patent saturation and formal intake remain incomplete.
