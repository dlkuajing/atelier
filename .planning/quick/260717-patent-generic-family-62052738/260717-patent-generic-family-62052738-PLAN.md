# Quick Task: Patent Generic Family 62052738

**Status:** complete-shovel-saturation-incomplete
**Parent:** patent saturation engineering (active / incomplete)
**Started:** 2026-07-17
**Entry evidence:** strict 619/619 replay ledger with
`generic_summary_metadata_missing` at 144 items / 144 roots and no corrupt result.

## Objective

Resolve deterministic next Family ID `62052738`, roots `US-10739565` / `US-20190154990`, and
same-application publications `US-10739565-B2` / `US-20190154990-A1`. Reconcile exact
publication/application/family identity, every disclosed
prescription and auxiliary table, system metadata, finite/infinite conjugate language, asphere
definition, and complete official PDF denominator before allowing conversion or a source-proven
terminal/nonterminal outcome.

## Plan

1. Freeze the 144-item generic census, exact official HTML identity, application/priority/family
   relationships, same-application publications, and attempt-1 document-level failure.
2. Establish exact embodiment, surface/asphere, inflexion/arrest-point, condition, figure, section,
   paragraph, and claim denominators. Bind every table cell and optical-system metadata statement.
3. Retrieve the exact B2 official PDF independently twice, inspect every decoded page/table/drawing
   sheet, cross-check the same-application A1 and an independent public mirror without borrowing
   numeric truth across publications, and queue outside-cohort family members.
4. Add one exact-source parser or terminal/nonterminal profile only if every disclosed item and
   required field is accounted for. Preserve source values and fail closed on absent, malformed,
   conflicting, or derivation-only metadata; do not convert a source-wide F-number inequality into
   an exact embodiment value.
5. Replay the root append-only twice, compare canonical semantics after only permitted retry/
   receipt normalization, audit all 619 roots, and rebuild the after generic census twice.
6. Run offline patent/guard tests, Ruff, Python compilation, and `git diff --check` with
   `PYTHONUTF8=1`, active non-`real_machine` guard, and CODE V inventory zero.
7. Update STATE, decisions, source/raster evidence, external-family queue, and recovery cursor.
   Commit this shovel, then remeasure every executable bucket. Parent saturation remains
   incomplete.

## Entry facts

- Queue order identifies `US-10739565-B2`, Family ID `62052738`, with layout signature
  `112421b530d15777f5ea4cbac7ddd93b6f03ea1c034ff34acbe011bb6a6c9613`.
- Same-application prior publication `US-20190154990-A1` is also a frozen cohort root; its layout
  signature is `274ab70b85e649ad2487e6f47280d82c89897058332cdc1f4f5f7cb672890e8c`.
  Both attempt-1 results are document-level `generic_summary_metadata_missing` parser reviews with
  `PatentParseError: embodiment f/Fno/HFOV line not found`.
- Retained official source path is
  `data/patent-lake/uspto-ppubs-html/USPAT/c53ccd5b2c25bd7a/US-10739565-B2.html`;
  raw SHA-256 is `c53ccd5b2c25bd7adc2f224be00fbfe671c5e23b1771671c4f508d6b00a2657b`.
- Source metadata identifies application `16/101656`, AAC ownership, prior publication
  `US-20190154990-A1`, and Chinese priority `2017 1 1143573` dated 2017-11-17.
- Preliminary normalized source inspection finds two five-lens embodiments, twelve PPUBS tables,
  exact focal lengths, curvature/thickness/index/Abbe rows, even asphere coefficients A4-A16,
  pupil diameters, image heights, and diagonal vision-field angles. The text publishes only a
  source-wide aperture F-number upper bound (`<= 2.0`) rather than an exact embodiment F-number.
  These are entry observations, not yet a parser or terminal decision.
- Entry worktree is clean at commit `8a3c180`, branch is 0 behind / 88 ahead of `origin/main`, and
  CODE V inventory is zero.

## Completion evidence

- Exact official B2/A1 HTML binds application `16/101656`, Family ID `62052738`, Chinese
  priority `CN201711143573.X`, two five-lens embodiments, TABLES 1-12, and FIGS. 1-8. Raw and
  normalized source hashes, section/paragraph/claim denominators, every formal table, focal row,
  surface/material/asphere/auxiliary row, and direct system-metadata statement are fail-closed in
  `scripts/patent_to_zmx.py` and rehashed from retained evidence.
- Two B2 official wrappers and the A1 official/Google pair are each 12 pages. The two B2 wrappers
  agree on every decoded page raster, as do the two A1 wrappers; B2 and A1 have no equal
  same-position page raster and no numeric truth is borrowed across publications. Drawing sheets
  occupy pages 2-6 and the specification/claims boundary is retained on pages 11-12.
- Each publication independently expands to two exact
  `metadata_unpublished.system_f_number_absent` terminals. Tables publish direct focal lengths,
  complete five-lens/filter prescriptions, eight even asphere coefficients per R1-R10 surface,
  entrance-pupil diameter, image height, and diagonal field angle, but publish only the inequality
  `F-number <= 2.0`; no exact F-number is derived from focal length and pupil diameter.
- Root-specific attempts 2/3 are semantically deterministic for both frozen roots. Result set is
  `7a070df9b865d53ff89cba1cfa0f1242b11f250d1602b65eafbbb5bcd1be3ed6`, summary is
  `ffa0e9c8e7313aa8c497d4ab258ce356e9476de58685625f73d9a462c2f9443c`, report is
  `0af362b282476114fce3447ef010d788345a3fe9029f10ad93d07087624e44c2`, and strict audit is
  619/619 with missing=0/corrupt=0. Generic falls 144 to 142 roots/items; the twice-built after
  census is byte-identical at
  `013ab6f60adfb6db9f08794066d587eb9625bc5e261b6fbfcacd9525b5deee33`.
- Four focused source/raster/replay tests pass after the format-noise cleanup; the pre-cleanup full
  offline patent/guard selection passed 459 tests. Final Ruff/compile/focused/audit/diff checks and
  CODE V zero-inventory verification are repeated immediately before commit.
- CN/JP family publications are retained in the outside-cohort queue. Deterministic ordering now
  selects Family ID `94531539`, root `US-20250189695`, publication `US-20250189695-A1`.
  External-family closure, remaining parser buckets, staging intake, macro replay support, and
  source exhaustion remain open; the parent patent-saturation goal is not complete.
