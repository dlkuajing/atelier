# Quick Task: Patent Generic Family 87936009

**Status:** complete-shovel-saturation-incomplete
**Parent:** patent saturation engineering (active / incomplete)
**Started:** 2026-07-16
**Entry evidence:** strict 619/619 replay ledger with
`generic_summary_metadata_missing` at 151 items / 151 roots and no corrupt result.

## Objective

Resolve deterministic next Family ID `87936009`, root `US-12591118`, publication
`US-12591118-B2`. Reconcile exact publication/application/family identity, every disclosed
optical or mechanical state, all figures/tables, required system metadata, and the complete
official PDF denominator before choosing deterministic conversion, source-proven terminal
outcomes, or continued parser review. The shared layout signature is queue evidence only and
does not authorize borrowing from Family `85407590`.

## Plan

1. Freeze the 151-item generic census, official HTML identity, family/application/priority facts,
   and attempt-1 document-level failure.
2. Establish the exact embodiment/state denominator from headings, narrative, tables, and
   drawings; distinguish optical prescriptions from barrel/carrier/drive/device architecture.
3. Retrieve the official PDF independently twice, inspect every page/drawing/table sheet, and
   durably queue directly evidenced publications outside the frozen cohort.
4. Add one exact-source parser or terminal profile only if every disclosed state and required
   field is accounted for. Preserve source values and fail closed on malformed, conflicting, or
   absent cells; never borrow from the other family sharing the layout signature.
5. Replay the root append-only twice, compare canonical semantics excluding only permitted retry
   identity, audit all roots, and rebuild the after generic census twice.
6. Run offline patent/guard tests, Ruff, Python compilation, and `git diff --check` with
   `PYTHONUTF8=1`, active non-`real_machine` guard, and CODE V inventory zero.
7. Update STATE, decisions, source/raster evidence, external-family queue, and recovery cursor.
   Commit this shovel, then remeasure every executable bucket. Parent saturation remains
   incomplete.

## Entry facts

- Queue order identifies `US-12591118-B2`, Family ID `87936009`, with layout signature
  `076d89457ec7f8b83b784c2a31d4511edb84e3f392d952f2b369cca38892f2c3`.
- That layout signature was previously shared by Family `85407590`; exact family-local evidence
  remains mandatory.
- Post-commit strict audit is 619/619 with corrupt=0; worktree is clean, branch is 0 behind / 81
  ahead of `origin/main`, and CODE V inventory is zero.

## Completion evidence

- Exact B2 HTML plus two independently fetched B2 wrappers and two independently fetched
  same-application A1 wrappers bind five embodiments, ten mechanical states, FIGS.1-23, and all
  16 source tables. All 43 decoded page rasters agree within each publication.
- The root now contains five non-working metadata terminals, two malformed-source metadata
  terminals, two receipt-classified trace failures, and one staging-only conversion. Attempts
  3/4 are semantic-equal at `fbdc95cc...6fafd`.
- Generic census is byte-identical across two rebuilds at 150 roots/items; result set is
  `9acd2262...bd35`, strict audit is 619/619 with corrupt=0, and the next exact group is Family
  `95157884`, root/publication `US-20250216652` / `US-20250216652-A1`.
- 415 offline patent/guard tests, Python compilation, Ruff, and `git diff --check` pass with
  CODE V inventory zero. Parent saturation remains active and incomplete.
