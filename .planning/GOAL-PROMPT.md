# 新 session 起手提示词（Atelier · 自主推进至北极星）

> 用法：新开 session 时把本文件**全文粘贴**为第一条消息（或用 `/goal` 载入）。
> 维护：每次交接由当班 AI 更新 §4 / §5，其余尽量不动。
> 最后更新：2026-07-29（P2 三把尺子轮 · 主公裁定投入扩底库）

---

你负责把 Atelier 推进到北极星达成。按下面的契约自主连续工作，遇红线才上交。

## 0. 先重建上下文（按序，不要跳）

1. 本文件 §4 当前状态 / §5 下一步 / §3 工作纪律
2. `.planning/NORTH-STAR.md` —— **唯一目标真相锚**，与任何文件（含本段）冲突以它为准
3. 根 `CONTEXT.md` —— 术语表，用它的词
4. `AGENTS.md` + `CLAUDE.md`
5. memory 索引 `MEMORY.md`
6. `.planning/decisions.log` 末尾 10 条
7. `git log --oneline -15` + `gh pr list --state open`

**本文件里任何事实陈述都可能已过期。与仓库/运行时证据冲突时以后者为准，并顺手更正本文件。**
2026-07-28/29 两轮共十余条陈述被实测推翻，**其中大半是当班 AI 自己写下的**。

## 1. 目标

把手机镜头「出一版设计」自动化：结构化 spec → 四件套交付物（处方 ZMX / 像质 /
公差良率 / 相对成本指数），零人工介入。主指标 = **异源打平率**。
`N`、门槛、`T` 三个数值**留空待实测，禁止先验拍板**。

**可信度是唯一不可失守的东西。宁可报「不可测」，不可报一个看起来漂亮的假数。**

## 2. 自主度

**自己决**：单文件改 / 测试 / lint / 读码答问 / 执行已确认方案 / 每铲做完自选下一铲 /
审查强度与轮数 / **CI 绿即合并**（`AI Release Authority` 已授权）/ **真机 CODE V 跑批**
（不是上交项，只需守红线①）。中影响以上的可逆自决，写一行 `.planning/decisions.log`。

**必上交**：改北极星或目标定义 / 改全局配置 / **造件 ahead-of-consumer** /
不可逆且无快速回滚 / `[EXPERT]` 代填 / 翻无人值守开关 /
**改 `SCENARIO_BOUNDS`**（与官网滑块对齐的 drift 契约，改要两边同步）。

**红线①（CODE V 单实例）**：真机跑批**前后**都要证 `codev`/`codevm` **会话数为零**——
判据是**会话数不是进程数**，正确判据为「父进程不是 codev/codevm 的 codev/codevm 进程数」。
主公的 Codex app-server 常驻，**绝不要杀 codex.exe**。
现成实现：`scripts/p2_crosssource_trial.py` 的 `codev_sessions()`。

## 3. 纪律（每条都是真金白银换的，别省）

### 先用工具，别猜

- **推送前必跑 `scripts/impact_slice.py --base origin/main --pytest`**，按它给的清单跑测试。
  **别猜哪些测试相关**——猜错一次实测代价 **57min 失败 run + 57min 重跑 ≈ 114min**
- **施工走 loop-shovel 轻卡**：干净 `origin/main` 切 worktree → 原子提交 → 机器闸判绿 →
  `decisions.log` 一行

### CI 的真实成本

- `main` **没有分支保护**（私有仓非 Pro）——「CI 绿才合」是**约定不是闸**。
  合并前必须自己核 `gh run view <id> --json conclusion`；`gh pr merge` 没报错**不等于**过了
- **一个 PR 只推一次**。`concurrency: cancel-in-progress: true` 会 cancel 掉正在跑的那轮，
  每多推一次把 **57 分钟**清零。证据文档 / `decisions.log` / 交接单**攒成一次推送**
- ⚠️ **「纯文档 PR 跳过 CI」只对整个 diff 全是文档的 PR 成立**——
  `pull_request` 触发按**整个 PR 的 diff** 比 `paths-ignore`
- **CI 串行，别加 `-n`**（三次实测证伪；runner 实测 `nproc=2`）

### 测量陷阱（每条都中过招）

- **报比例前先确认分母的边界与成分**。这两轮栽了两次：先是**边界**（底库三个池），
  后是**成分**——24 条对照里 20 条的 spec 会被产品自己的参数闸拒收，
  而**赢的两条恰好全在域外**，8.3% 因此作废
- **比总数不能推重叠**，断言「不重叠」必须比**集合**
- **小样本的完美一致是假象（已中三次）**：8/8→92.9%、218/442→456/1055、n=9→n=53 方向翻转
- **退化值恰好等于理想读数（已中八次）**：新增任何指标先问「它失败时返回什么」。
  判据分**自证型**（正定量，单值可判）与**联合判据**（0.0 物理合法，须靠独立判别器）。
  **修一个假绿可能关掉另一个假绿的判别器**
- **第 8 例是精度不是哨兵**：CODE V `BUF EXP` 在 `1e-6~1e-4` 带按定点 6 位小数导出，
  高阶非球面系数最坏被削 **19%**，而值看上去就是个普通小数
- 读 ZMX **必须按 BOM 解码**；枚举用 `find data -iname "*.zmx"`（**必须 `-iname`**）

### 验证纪律

- **新测试必须先证明能失败**：拿到未修复代码上跑，失败才算有效捕获；
  旧码上也通过的，如实标注「契约钉」，不计入
- **自建探针先做阳性对照**，且用生产入口验证——更要问
  **「哪个生产入口才是本次改动的消费者」**。这一轮用 `run_codev_readout` 证了往返，
  但候选走的是 `codev_optimize` 的发射点，结果那次修复贡献实测为 **0**
- **两个发射点必须一致**：`codev_readout` 与 `codev_optimize._optimized_readout_block`
  写的是同一个解析器读的文件。已有通用守卫（surface key 集合必须相等），别绕过
- **`| tail -N` 会把输出全缓冲住**。长任务看似挂死先看进程 CPU
  （`Get-CimInstance Win32_Process`）。批量跑外部引擎**每项独立子进程 + 硬超时**
- **对抗审查当尽力而为，别排进关键路径**；没交付时走「自审 + PR 留证」，
  但**必须显式说明未交付**
- **本机测试**：`PYTHONUTF8=1`；新 worktree 无 `.venv`，用
  `PYTHONPATH=<worktree>` + `D:\atelier\.venv\Scripts\python.exe -m pytest`

## 4. 当前状态（2026-07-29）

`origin/main` = **`486fcc86`**（#105–#117 已合，各自 merge 后 main CI 均实测 success；
**#118 / #119 在飞**，见 §5.4）。

### 🔴 主指标 P2：域内 0 打平，且样本量不成立

**别引用任何历史 P2 比率。** 4.2% / 8.3% / 16.7% **全部作废**——
分母混着**产品会拒收的 spec**。

底库需要**三把尺子**（此前以为两把）：

| `data/zmx` 442 颗 | |
|---|---|
| 严格可追迹 ∧ 保真度干净 | 192 |
| **再过产品参数闸（第三把）** | **55（28.6%）** |

越界项：**FOV 88 ｜ EFL 60 ｜ IMH 44 ｜ F/# 32 ｜ 片数 31**。
底库 `scenario` 标注比 `SCENARIO_BOUNDS` **松得多**（全视场 133.8° 被标 ultrawide，
上界 105°）。

**P2 样本量上限 167 → 30，且只有 5–8 颗独立 seed（top-5 占 90%）。**
→ **P2 在域内目前不具备可报的样本量**——不是数字不好看，是分母不成立。

### 本轮修掉的四处交付物缺陷（都已合入 main）

| PR | 缺陷 | 实测影响 |
|---|---|---|
| #105 | 候选 ZMX 文件名挡住重新导入 | 每颗交付的候选都打不开 |
| #106 | 高阶系数导出截断 | **两颗候选好 60× / 117×** |
| #107 | `SPS ODD` 非球面被压成球面 | 但**只改 readout，对候选贡献 0** |
| #108 | #107 漏接 optimize 发射点 | 11 条「产不出候选」**全部回收** |

### 已知但未解

- **重建件 MTF 相对差 1.6%** 未追根因（EFL/RMS 都在 1e-4 内）
- **只往返过 1/158 颗 XASPHERE seed**
- **AUT merit 没有畸变操作数**——优化器在优化 P2 三项判据里的两项。
  全局权重已实测**否决**（一条几乎免费、一条得不偿失、一条 RMS 差 **165×**；
  且「权重越大 RMS 越差」不成立，优化面非凸）

## 5. 下一步（主公 2026-07-29 已裁定：**投入扩底库**）

1. **扩域内底库（已批，最高优先）** —— 当前**唯一**能解开 P2 的上游。
   目标：把「三把尺子全过」的 55 颗做大到足以支撑有统计意义的打平率。
   - ✅ **「先量」已做，实数**（`.planning/evidence/staging-domain-ceiling-2026-07-29.md`）：
     staging 613 → ①可追迹 238 → ②∧保真度干净 **205**，其中
     **域内 58 / 域外 121 / 仍无法判 26**，可判命中率 **58/179 = 32.4%**。
     **域内底库合计 55+58 = 113（约两倍）**
   - ⚠️ **该文件被改过两次，别引用任何 40／95／96／19.5%／46.6% 的旧说法**
   - 🟡 **「按域采」支撑**：可判 179 颗里 **121 颗（67.6%）落在产品域外**，
     三分之二转换产出用不上——值得在 USPTO 检索/转换阶段加域筛
   - ⚠️ **113 只是量级参考**：staging 用专利**声明值**、`data/zmx` 用**实算值**，
     不是同一刻度；且**未含 IMH 判据**（加上会让 58 变少）
   - ⚠️ **55 里只有 40 颗是专利，且有 30% 同族冗余 → 独立设计仅 28 颗**
     （2026-07-29 实测）。`US-11906710-B2-eN` 与 `US-20240201471-A1-eN` 是同一设计的
     授权本与申请公开本，spec 逐位相同；另有一组 4 条同 spec。
     **扩库计数必须按「独立 spec 元组」去重，不能数文件数。**
     ✅ 顺带**验证了一个否定结论**：同族排除**工作正常**——上述所有同 spec 对
     `brand_of_case` 都判为同品牌（LARGAN）而被正确排除，**没有 fail-open**。
   - 仍未做：26 颗无法判；IMH 判据未接；转换制品 752 与 USPTO 续采未按三把尺子评估；
     未经真机复核

2. **P2 重测（扩库后再做，否则白跑）** —— 对拍器 `scripts/p2_crosssource_trial.py`
   已就绪并跑过四轮；`p2_pair_census` 第三道筛**默认开启**。
   报数**必须同时给两个分母**（全部 trial / 仅可判定）与**独立 seed 数**
3. **产出能力 P1** —— 域外那轮 50% 不可判：`aut_not_converged` 5（EFL 差 10–40%）、
   候选产出但度量塌缩 6、超时 1。扩库后重新分类
4. **四件套只缺最后一件：公差良率**（相对成本指数已于 PR #115 合入）。
   **2026-07-29 又往下挖了两层，结论变了——下面的替代 §5.4 旧文，别引用旧诊断。**

   ✅ **解析层已修（PR #118，在飞）**：PER 导出有**两种表头，列数相同（18 vs 18）**，
   只差第 4/5 列（MTF 有 `Frequency`/`Azimuth`，RMS 波前留空）。已按 metric 走两套
   列布局，两侧 fail-closed。真机导出端到端可解析（2 条 PER + 40 条 MC）。

   ✅ **假完美读数已 fail-closed（PR #119，在飞）**：`ideal_reading_count` 按判据
   **方向**取完美值（越小越好→0.0，越大越好→1.0），有一条即拒，
   **不经 `max_saturation_fraction`**——那是容忍度旋钮，实测 `=0.2` 时对 10% 污染
   照样报 `yield=0.25`。⚠️ 顺带发现**测试套件原本把一个假良率钉成断言**
   （`per_field_yield == 0.9`，由 31 条不可能的 `MTF=1.0` 堆出来）。

   ❌ **真正的墙有两堵，都是代码/设置层，与镜头无关**：

   a. **我们根本没在设置公差。** `DLT S1..n <value>` / `DLR` 的量值被 CODE V
      **完全忽略**：同一序列把值从 `0.001` 改到 `0.3`（**300 倍**），`.lis` 的
      applied 列**逐字节相同**（`0.0200000v`，`v` = 取自默认限值表；S1 甚至是 0.5）。
      CODE V 只报一句无关的 image/object surface warning，命令是被接受的。
      **⇒ 至今每一次 TOR 跑的都是出厂默认公差**，序列自述的
      "uncalibrated mobile-lens starter set" 双重失实——不只是未标定，是没生效。
      → 下一步：查 CODE V 手册 TOR/TLI 章节的正确量值语法，**别猜**。

   b. **10.3% 的 MC 读数恰好 0.0**，根因未明。6 颗**域内**专利 × 两种补偿器配置，
      **78/760**，逐视场分散、6 颗全中。RMS 判据下 0.0 = 完美值 = 物理不可能。
      **不是**解析器（CODE V 原始导出里就写着 `0`）；**不是**镜头差
      （标称 RMS 波前 0.09–0.11 波，接近衍射极限）。
      PR #119 的护栏会在**全部 6 颗**上触发 ⇒ **按现状良率在全库不可测**。

   ⛔ **三条已被推翻的说法，别再捡**：
   - 「拦路虎是 `_parse_per` 不支持真机格式」→ 已修，不再是卡点
   - 「补偿器无界把判别力吃光」→ 域内实测 **cmp 2/120 vs nocmp 5/120**，无病态放大。
     那个 13/20 是 `US-12124006-B2-e2` **单颗镜头自身离焦 0.13mm**，
     且该颗**过不了产品参数闸**（EFL 2.222 < 2.3、FOV 132° > 105°）——
     我拿产品会拒收的镜头测了半天，与 P2 那次是同一类错误
   - 「零值与扰动量无关」→ 那四档实验因 (a) **根本没有变量**，结论作废

   公差表与成本表同理：绝对值不准不影响排序，**但必须两侧同表**；
   ⚠️ 该论证只在误差是**一致单调畸变**时成立，**测量饱和时排序不被保留**。

## 6. 待主公裁定（AI 不得自决）

1. **畸变判据的循环论证**：P2 judges 畸变，但 §1.1 输入 spec **没有畸变**。
   最自然的修法（畸变 ≤ 对照畸变）会**把对照数值泄进候选构造**，
   与北极星明令防范的循环同形。出路 A=把畸变预算纳入输入 spec；
   B=继续作软目标但须定一个**不看对照**的选优规则
2. **`NORTH-STAR.md` §3 的 P2 行已与实况不符**（仍写「待建对拍器」「零数据」），
   红线②禁止 AI 自改
3. **CI 吞吐三项**：merge 后 main 那轮是否不再阻塞发车 / 是否换 4–8 核 runner /
   要不要啃占全套 21% 的单个测试（714s）。
   ⚠️ 实测：`uv run` 开销仅 0.03s、光学栈导入 ≤0.25s，**换机器只能线性摊薄**，
   不如先挖重复计算

## 7. 汇报口径

每完成一铲简短汇报：**做了什么 / 判据绿否 / 哪些结论被实测推翻**。
数字**必带分母与口径**。未做的、未验证的、只做了一半的，**明说**。
发现自己先前的结论错了，**主动更正并说明证据**，不要悄悄改口。
